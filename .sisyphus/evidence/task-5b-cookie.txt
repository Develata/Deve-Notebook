Task: T5b Cookie secure policy hardening
Date: 2026-03-06

Changed file:
- apps/cli/src/server/auth/handlers.rs

Manual inspection:
- `build_auth_cookie` reads `HTTPS_ENABLED` with `std::env::var("HTTPS_ENABLED")`
- Truthy values are `true` and `1`
- Default is `true` when env var is absent
- Cookie builder explicitly sets `HttpOnly`, `SameSite::Strict`, and `.secure(https_enabled)`

Acceptance evidence for AUTH-003:
- Preconditions: `HTTPS_ENABLED=true`
- Expected Set-Cookie attributes: `Secure; SameSite=Strict; HttpOnly`
- Code path now satisfies this because `.secure(https_enabled)` is true under the precondition and the other two attributes are always set

Verification:
- `cargo clippy --package deve_cli -- -D warnings` -> passed
- `cargo test --package deve_cli` -> passed
