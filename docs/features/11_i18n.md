# 11_i18n.md - 多语言体验篇

本章描述界面语言、错误文案和语言切换的用户体验。

原子操作示例：[`operations/i18n_locale_error.md`](./operations/i18n_locale_error.md)

细粒度操作链：
[`locale_surface_switch.md`](./operations/locale_surface_switch.md),
[`i18n_locale_selection.md`](./operations/i18n_locale_selection.md),
[`i18n_error_mapping.md`](./operations/i18n_error_mapping.md),
[`i18n_localized_formatting.md`](./operations/i18n_localized_formatting.md),
[`i18n_hardcoded_audit.md`](./operations/i18n_hardcoded_audit.md)

## 功能目标

- 用户应能在支持语言之间切换界面。
- 错误提示、状态提示和核心控件文案应保持一致与可理解。

## 功能项

### 1. 语言切换

- 用户可以切换当前界面语言。
- 切换后主要界面文案应立即更新。

### 2. 文案覆盖

- 核心工作流中的标题、按钮、状态、错误提示应有一致语言覆盖。
- 登录页作为认证入口，其标题、输入标签、按钮和认证错误提示也属于核心文案覆盖范围。
- 顶部 Header、底部状态栏与移动端 footer 对同一连接/同步状态应复用同一 i18n 文案来源。
- 状态栏加载中与加载进度文案应通过统一 i18n facade 输出，窄屏 compact 文案也应随语言切换。
- 不应出现大面积中英混杂或遗漏键。

### 3. 错误语义一致

- 同一类错误在不同位置应保持语义一致。
- 用户看到的是可理解文案，而不是后端内部错误码。

## 非目标

- 当前阶段不要求所有历史归档文档或开发文档都本地化。
- 当前阶段不允许前端直接显示后端裸自然语言错误作为稳定协议。

## Chrome MCP 验收实例

### I18N-FEAT-01: 语言切换与错误文案

前置条件：

- 打开应用并进入主工作流。

步骤：

1. 打开 Settings 或命令入口切换语言。
2. 查看 Header、Sidebar、Source Control、状态栏文案。
3. 再触发一个明确错误或受限状态。

期望结果：

- 主要文案同步切换。
- 错误提示可理解且与当前语言一致。
