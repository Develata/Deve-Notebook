<!-- Generated: 2026-05-20 -->

# Runtime Skeleton Registry

## Status

本文件是 runtime 名称、收敛状态、当前代码承载路径与 tracking task 的受控登记表。
它连接 `docs/plan/` 的 refactor target、`docs/tasks/18_infra_runtime.md` 的责任带
与当前代码，但不替代任何 plan 章节的规范性约束。

## Rules

- `runtime` 名称必须与 plan 章节中的 `Refactor Target` 或本表既有名称一致。
- `status` 只能使用下表枚举。
- `current_module_path` 必须指向当前真实承载位置；分散承载时列主要路径，不写
  尚未存在的目标路径。
- `tracking_task` 指向当前迁移蓝图；没有明确任务时写 `待分配`。
- 代码路径迁移、runtime 独立成模块、或状态跨档时，必须同步更新本表。

## Status Values

| Status | Meaning |
|---|---|
| `已收敛` | 已有独立命名模块或窄模块组，边界与 runtime 名称基本一致。 |
| `部分承载` | 行为已存在，但分散在多个模块，或模块名尚未按 runtime 收敛。 |
| `未启动` | 尚无稳定代码承载位置。 |
| `抽象分层` | 当前是跨模块架构层，不应假装已有单一 runtime 模块。 |

## Runtime Registry

| Runtime | Status | Current module path | Tracking task | Boundary |
|---|---|---|---|---|
| `authority_storage_runtime` | `部分承载` | `crates/core/src/ledger/manager/authority_storage_runtime.rs`; `crates/core/src/ledger/manager/prepared_change_batch.rs`; `crates/core/src/ledger/manager/prepared_change_batch/` | `docs/tasks/18_infra_runtime.md` | Ledger append validation、host-bound local fact writer、peer fact sequence 与 authority table 边界；B3 已将 External Apply 与 Remote Import 收敛到不可复制、crate-private、source-specific sealed `PreparedLedgerChangeBatch`。ownership-aware RemoveLocalRepo 所需 exact RepoId authority-use admission、retirement/exclusive proof 尚未落地；当前不可替换 bootstrap `local_db` 和分散 `Arc<Database>` 持有是 active drift。 |
| `repo_mutation_publication_runtime` | `已收敛` | `apps/cli/src/server/repo_mutation/` | `待分配` | process-scoped RepoId permit、local authority writer 提交结果分类与 ordered publication enqueue；watcher、remote shadow ingest、offline repair/export/diagnostic 排除。 |
| `projection_persistence_runtime` | `已收敛` | `crates/core/src/sync/projection_persistence_runtime.rs`; `crates/core/src/projection_fault/` | `docs/tasks/18_infra_runtime.md` | 顶层 storage infra：从 ledger fold 派生 projection、workspace writeback 与 drift explanation；唯一拥有 repo-local Redb v4 `PROJECTION_FAULTS` typed store，并向 authority-specific coordinator 提供 same-transaction fault primitive；Remote Import runtime 仍唯一拥有 receipt CAS。该 side table 不是 Ledger Fact 且不同步。通过 typed boundary 使用其 `projection_locator_runtime` 子 runtime。 |
| `projection_locator_runtime` | `部分承载` | `crates/core/src/ledger/manager/projection_locator.rs` | `docs/tasks/18_infra_runtime.md` | 唯一拥有 locator v2 query/admission/conflict 与 prepared-create API；Alias 永不进入 locator。RemoveLocalRepo 所需 manifest-bound row token、membership-revoked conditional delete 与 exact repair revalidation 尚未落地。 |
| `watcher_runtime` | `部分承载` | `crates/core/src/sync/watcher/`; `apps/cli/src/watcher_runtime.rs`; `apps/cli/src/server/runtime/watcher_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_runtime/` | `docs/tasks/18_infra_runtime.md` | 已具备 core owned `RepoWatcherHandle`、capture-first startup、host-owned `WatcherSupervisor`、exact-slot `WatcherRuntimeView` admission、worker failure cut、全部在线 workspace-dependent writer 的 mounted gate、W5 public aggregate health、W6 E2 final-state shutdown，以及 A1/B1 create/remove prepare-cut-settle/provider coordination。W8 repo isolation、W9 Windows overflow producer与W10跨平台证据尚未完成，因此整体 watcher runtime 保持部分承载。 |
| `repair_runtime` | `已收敛` | `crates/core/src/ledger/manager/repair_runtime.rs` | `docs/tasks/18_infra_runtime.md` | Degraded、quarantine 与 repair action 边界。 |
| `projection_repair_runtime` | `已收敛` | `crates/core/src/sync/projection_repair_runtime.rs` | `docs/tasks/18_infra_runtime.md` | Projection rebuild、projection repair 与 repair diagnostics。 |
| `repo_catalog_runtime` | `部分承载` | `crates/core/src/ledger/manager/repo_catalog_runtime.rs`; `crates/core/src/ledger/manager/repo_catalog_runtime/` | `docs/tasks/18_infra_runtime.md` | RepoId identity、per-RepoId membership、normal listing、prepared create/remove cut与 generation 已接入。RemoveLocalRepo cleanup 成功后的 exact transient-tombstone retirement、manifest binding 与 restart repair API 尚未落地。 |
| `host_repo_alias_runtime` | `部分承载` | `crates/core/src/ledger/manager/host_repo_alias.rs`; `crates/core/src/ledger/manager/host_repo_alias/`; `apps/cli/src/commands/repo_alias.rs`; `apps/cli/src/server/handlers/repo_control.rs` | `待分配` | 唯一拥有 alias store/CAS/JSON import-export/typed warnings；CLI/Web 只消费 typed API。RemoveLocalRepo 所需 membership-revoked、revision-bound conditional row cleanup与 exact repair revalidation 尚未落地。 |
| `repo_lifecycle_job_runtime` | `部分承载` | `apps/cli/src/server/runtime/repo_lifecycle_job_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_job_runtime/`; `apps/cli/src/server/runtime/repo_lifecycle_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_runtime/` | `待分配` | A1 逐 request durable receipt、normalized-intent idempotency、bounded transport-independent worker、restart recovery hook与显式 shutdown/join已接入 B1 per-RepoId prepare/short cut/settle；observer publication绑定 exact session scope。ownership-aware RemoveLocalRepo 的 exact manifest、DB handle retirement、owned-state deletion与 cleanup repair 尚未落地，因此不得签署收敛。 |
| `repo_control_client` | `部分承载` | `apps/web/src/runtime/repo_control_client.rs`; `apps/web/src/hooks/use_core/effects/message_dispatch_route_control.rs` | `待分配` | F4/v4 thin client的 exact scope/job复核与 typed error投影已落地；ownership-aware RemoveLocalRepo 的准确 destructive confirmation 与 cleanup-debt projection 尚未接入，前端仍不得推断 inventory或恢复策略。 |
| `repo_scope_runtime` | `已收敛` | `crates/core/src/ledger/manager/repo_scope_runtime.rs` | `docs/tasks/18_infra_runtime.md` | Branch、`scope_nonce` 与 writable state。 |
| `session_runtime` | `部分承载` | `apps/cli/src/server/auth/handlers/session.rs`; `apps/cli/src/server/ws/auth.rs` | `docs/tasks/18_infra_runtime.md` | User session 与 WebSocket session lifecycle。 |
| `auth_gateway` | `部分承载` | `apps/cli/src/server/auth/` | `docs/tasks/18_infra_runtime.md` | 当前承载 HTTP/WS 入口鉴权、cookie/JWT 与安全头；B4 已新增 loopback-only `LocalCliProxyAuthority`，只从 current JWT bearer + exact request identity 构造 Remote Import capability，不复用 Cookie-only/dev session/browser/P2P/delegated grant。 |
| `server_runtime_assembly` | `部分承载` | `apps/cli/src/server/runtime/`; `apps/cli/src/server/start.rs`; `apps/cli/src/server/setup.rs`; `apps/cli/src/server/router.rs`; `apps/cli/src/server/static_files.rs`; `apps/cli/src/server/metrics.rs`; `apps/cli/src/server/p2p/`; `apps/cli/src/server/prewarm.rs`; `apps/cli/src/server/tree_state.rs` | `docs/tasks/19_repo_refactor_blueprint.md` | Server composition root、auth/plugin/sync/watcher/tree/metrics/static-router 装配；handlers 不得承担 runtime assembly。 |
| `browser_auth_runtime` | `部分承载` | `apps/web/src/app/auth_monitor.rs`; `apps/web/src/api/auth_probe.rs` | `docs/tasks/18_infra_runtime.md` | Browser auth probe、session refresh 与 unauthorized recovery。 |
| `browser_identity_client` | `已收敛` | `apps/web/src/storage/capability.rs`; `apps/web/src/storage/identity.rs`; `apps/web/src/storage/js_bridge.rs`; `apps/web/src/hooks/use_core/storage_runtime/` | `待分配` | JS bridge 只探测 IndexedDB/WebCrypto Ed25519 与调用 non-extractable key API；Rust client 产生强类型 write blocker 并控制 identity readiness；UI 只投影 blocker 文案，不拥有密钥、签名实现或 writer authority。 |
| `transport_runtime` | `部分承载` | `apps/cli/src/server/handlers/sync/`; `apps/web/src/hooks/use_core/effects/message_runtime.rs`; `apps/web/src/hooks/use_core/effects/message_runtime_sync/` | `docs/tasks/18_infra_runtime.md` | WS/HTTP transport、message classification 与 protocol gate。 |
| `repo_scope_sync_runtime` | `部分承载` | `crates/core/src/sync/repo_scoped.rs`; `apps/cli/src/server/repo_scope/sync.rs`; `apps/cli/src/server/handlers/sync/hello/scope/` | `docs/tasks/18_infra_runtime.md` | Repo-scoped handshake、PeerFactSeq vector hydration、scope cleanup 与 stale discard。 |
| `session_client` | `部分承载` | `apps/web/src/runtime/session_client/`; `apps/web/src/hooks/use_core/effects/`; `apps/web/src/api/` | `docs/tasks/19_repo_refactor_blueprint.md` | Web transport/session readiness、handshake gate 与 reconnect coordination；client adapter，不保存业务真相。 |
| `relay_proxy_runtime` | `部分承载` | `crates/core/src/protocol/relay_proxy.rs`; `apps/cli/src/server/handlers/sync/transfer.rs`; `apps/cli/src/server/handlers/sync/snapshot.rs` | `待分配` | Relay/proxy route admission only；不得改写 payload source attribution。 |
| `scope_client` | `部分承载` | `apps/web/src/runtime/scope_client/`; `apps/web/src/hooks/use_core/`; `apps/web/src/hooks/use_core/effects_switch/` | `docs/tasks/19_repo_refactor_blueprint.md` | Browser repo/branch/doc scope、`scope_nonce`、scope prefs 与 stale-scope recovery；只消费 server/core authority。 |
| `document_client` | `部分承载` | `apps/web/src/runtime/document_client/`; `apps/web/src/runtime/document/`; `apps/web/src/editor/sync/`; `apps/web/src/editor/hook_runtime.rs`; `apps/web/src/hooks/use_core/effects/message_control.rs` | `docs/tasks/20_web_thin_client_ledger_migration.md` | Browser document snapshot、pending overlay、ack/reject、失败原子 replay、same-page internal reconnect/session restore scope rebind 与 navigation guard；client-side coordination adapter，adapter/replay 失败保持只读并要求新 generation/request Retry。 |
| `projection_recovery_client` | `已收敛` | `apps/web/src/runtime/projection_recovery/`; `apps/web/src/api/incoming.rs` | `待分配` | 执行后端指定的 typed refresh、incoming gap connection retirement、generation-bound reopen、pending 保留/单次 replay 与显式 Retry；不推断 affected docs 或 authority 结果。 |
| `pending_overlay_runtime` | `已收敛` | `apps/web/src/runtime/document/pending/` | `docs/tasks/20_web_thin_client_ledger_migration.md` | Browser session pending overlay；不得写入 `pending_fs_ops`。 |
| `write_confirmation_runtime` | `已收敛` | `apps/web/src/runtime/document/write_state.rs`; `apps/web/src/runtime/document/confirm.rs`; `apps/cli/src/server/handlers/document/write_confirmation.rs` | `docs/tasks/20_web_thin_client_ledger_migration.md` | Ack、reject 与 committed-but-writeback-failed 的生命周期分类合同；write readiness 由 handshake/scope 带前置门控，不在本带收口。 |
| `source_control_runtime` | `部分承载` | `crates/core/src/ledger/manager/source_control_runtime.rs`; `crates/core/src/source_control/` | `docs/tasks/18_infra_runtime.md` | 消费 `pending_fs_ops` / `GitImportRequested` 并生成 Deve stage/commit intent；从 commit anchor 到 ledger head 派生 confirmed ledger dirty。B4 已删除 Remote Projection pull 的 External Changes/Source Control foreign routing；本 runtime 的剩余 partial 与 Remote Import 无关。 |
| `external_changes_runtime` | `部分承载` | `crates/core/src/source_control/`; `crates/core/src/ledger/manager/commit_runtime.rs`; `apps/cli/src/server/handlers/source_control/` | `docs/tasks/18_infra_runtime.md` | Projection Workspace 外部偏差的 read/stage/unstage/discard/apply-to-ledger；不得创建 commit anchor、history、graph 或 Git mirror state。 |
| `diff_session_runtime` | `已收敛` | `crates/core/src/source_control/diff_projection/`; `apps/cli/src/server/diff_projection.rs`; `apps/cli/src/server/handlers/source_control/diff/`; `apps/web/src/components/diff_view/` | `docs/tasks/18_infra_runtime.md` | Core 计算 typed rows/hunks/folds/UTF-16 ranges；CLI 管理 bounded blocking、scope/revision/cancellation 与 commit-file on-demand；Web 只切片、虚拟化和发送 draft intent。Diff session 不拥有 ledger/Source Control authority。 |
| `merge_runtime` | `部分承载` | `crates/core/src/ledger/merge/`; `crates/core/src/ledger/manager/merge_ops.rs`; `apps/cli/src/server/handlers/merge/` | `docs/tasks/18_infra_runtime.md` | Merge lifecycle 只通过 local authority path 收敛；MergeAnchor 与 repo-local source/doc checkpoint 证明共同基线，前端不得计算或写入 merge authority。 |
| `remote_projection_transport_runtime` | `部分承载` | `crates/core/src/remote_projection/`; `apps/cli/src/remote_projection_transport.rs`; `apps/cli/src/remote_projection_transport/`; `apps/cli/src/commands/projection_remote.rs`; `apps/web/src/components/command_palette/registry/remote_projection.rs` | `待分配` | WebDAV/S3 locator/profile admission、credential、HTTP/signing、projection push 与 ordered source acquisition 已收口；push/source 使用分离 typed interface，provider 不拥有 session、Ledger 或 workspace mutation。B4 已把 source acquisition 接入 Remote Import 并删除旧 pull overwrite/scan；B6 fresh producer 前保持部分承载。 |
| `remote_import_runtime` | `部分承载` | `crates/core/src/remote_import/`; `crates/core/src/ledger/manager/remote_import_apply.rs`; `crates/core/src/ledger/manager/prepared_change_batch/remote_import.rs`; `apps/cli/src/remote_import_runtime.rs`; `apps/cli/src/remote_import_runtime/`; `apps/cli/src/commands/remote_import/`; `apps/cli/src/server/handlers/remote_import/` | `待分配` | Durable store/artifact/recovery/retention/repair、sealed Apply、ADR 0012 settlement、exact product API、Mounted admission、Local CLI proxy与 Ledger-to-Projection rematerialization 已落地；W7 provider quiesce/membership coordination 与 C1′ alias non-transmission evidence 尚未收敛，B5/B6 仍阻塞。 |
| `document_runtime` | `部分承载` | `apps/cli/src/server/handlers/document/`; `apps/web/src/runtime/document_client/`; `apps/web/src/editor/` | `docs/tasks/20_web_thin_client_ledger_migration.md` | OpenDoc、snapshot、history 与 edit intent；Web 端只作为 `document_client` adapter。 |
| `source_control_client` | `部分承载` | `apps/web/src/runtime/source_control_client/`; `apps/web/src/runtime/source_control_client/diff_session.rs`; `apps/web/src/hooks/use_core/callbacks_sc/`; `apps/web/src/hooks/use_core/callbacks_sc.rs`; `apps/web/src/hooks/use_core/callbacks_sc_scope.rs`; `apps/web/src/hooks/use_core/callbacks_sc_target.rs`; `apps/web/src/components/sidebar/source_control/` | `docs/tasks/19_repo_refactor_blueprint.md` | Browser source-control typed intent、diff session 与 readonly projection；不得拥有 stage/commit authority。 |
| `external_changes_client` | `部分承载` | `apps/web/src/runtime/external_changes_client/`; `apps/web/src/hooks/use_core/contexts/external_changes.rs`; `apps/web/src/components/sidebar/external_changes.rs`; `apps/web/src/components/sidebar/external_changes/` | `docs/tasks/19_repo_refactor_blueprint.md` | Browser External Changes typed intent facade；只展示外部投影偏差并发送 stage/unstage/discard/apply-to-ledger intent，不拥有 ledger/source-control authority。 |
| `remote_import_client` | `未启动` | `未启动` | `待分配` | 批准目标为与 Source Control、External Changes 同级且独立的 typed thin client；只绑定 scope/session/revision、渲染 backend label/diff/blocker 并发送 intent，不拥有 provider、diff、stale 或 apply 判断。 |
| `render_projection_runtime` | `部分承载` | `apps/web/src/runtime/rendering_client/`; `apps/web/src/editor/`; `apps/web/src/components/outline_render/`; `apps/web/js/katex_bridge.js`; `apps/web/js/rendering_bridge.js`; `apps/web/js/chat_math_bootstrap.js`; `apps/web/js/chat_math.js`; `apps/web/js/editor_lifecycle.js`; `apps/web/js/index_editor_adapter.js`; `apps/web/index.html` | `docs/tasks/18_infra_runtime.md` | Projection-only rendering state、chat math pass、KaTeX facade、host-owned CodeMirror mount/cleanup 与 render hints；`index.html` 只保留资源与脚本装载顺序，不承载 bridge implementation。 |
| `widget_bridge_runtime` | `部分承载` | `apps/web/src/runtime/rendering_client/`; `apps/web/js/web_bridge_registry.js`; `apps/web/js/widget_bridge.js`; `apps/web/js/katex_bridge.js`; `apps/web/js/rendering_bridge.js`; `apps/web/js/index_bootstrap.js`; `apps/web/js/index_editor_adapter.js`; `apps/web/js/extensions/`; `apps/web/src/editor/`; `apps/web/index.html` | `docs/tasks/18_infra_runtime.md` | Widget/editor/browser bridge 与 renderer extension，不拥有 authority；`window.*` 必须经 bridge registry 收敛，`index.html` 只保留 bootstrap script loading。 |
| `outline_projection_runtime` | `部分承载` | `apps/web/src/components/outline_render/`; `apps/web/src/hooks/use_outline.rs` | `docs/tasks/18_infra_runtime.md` | Outline projection only；不得成为 document authority。 |
| `ui_shell` | `抽象分层` | `apps/web/src/components/`; `apps/desktop/src/shell/`; `apps/mobile/src/shell/` | `docs/tasks/18_infra_runtime.md` | View intent、layout shell、panel/focus/stacking 管理。 |
| `application_control` | `抽象分层` | `apps/web/src/hooks/use_core/`; `apps/web/src/runtime/domain/`; `apps/web/src/runtime/session_client/`; `apps/web/src/runtime/scope_client/`; `apps/web/src/runtime/document_client/`; `apps/web/src/runtime/source_control_client/`; `apps/web/src/runtime/rendering_client/`; `apps/desktop/src/shell/`; `apps/mobile/src/shell/` | `docs/tasks/19_repo_refactor_blueprint.md` | Control 编排、typed intent routing 与 runtime 状态汇聚；Web `use_core` 必须降级为 composition root，runtime 共享 request/state 类型归 `runtime/domain`。 |
| `feature_runtime` | `抽象分层` | `apps/web/src/components/sidebar/source_control/`; `apps/web/src/components/command_palette/`; `apps/web/src/components/dashboard/` | `docs/tasks/19_repo_refactor_blueprint.md` | Feature-local state machines，不直接持有 authority。 |
| `native_shell_mode_runtime` | `部分承载` | `crates/core/src/native_adapter/`; `apps/desktop/src/service_entrypoint/`; `apps/desktop/src/service_entrypoint/git_executable.rs`; `apps/desktop/src/tauri_bootstrap/`; `apps/desktop/src/tauri_entry/`; `apps/desktop/capabilities/`; `apps/mobile/src/tauri_entry.rs`; `apps/mobile/src/tauri_entry/backend_recovery/`; `apps/mobile/capabilities/`; `apps/mobile/src/tauri_lifecycle.rs`; `apps/mobile/src/embedded_backend/`; `apps/mobile/src/embedded_backend/cookie.rs`; `apps/mobile/src/embedded_backend/supervisor.rs`; `apps/mobile/src/embedded_backend/supervisor_shutdown.rs`; `apps/mobile/src/embedded_backend/supervisor_webview.rs`; `apps/mobile/src/embedded_backend/generation.rs`; `apps/mobile/src/embedded_backend/supervisor_types.rs`; `apps/cli/src/native_runtime.rs`; `apps/cli/src/server/start.rs`; `apps/cli/src/server/runtime/peripheral_runtime.rs`; `apps/cli/src/server/ws/transport.rs`; `apps/web/src/api/connection.rs`; `apps/web/src/api/native_backend_bridge.rs`; `apps/web/src/hooks/use_core/effects/handshake/lifecycle.rs`; `apps/web/js/native_backend_bridge.js`; `scripts/check-desktop-packaged-ui-smoke.ps1`; `scripts/check-desktop-remote-browser-smoke.ps1`; `scripts/lib/webview2-cdp.ps1`; `scripts/smoke-desktop-packaged-ui.mjs`; `scripts/smoke-desktop-remote-browser.mjs`; `scripts/smoke-mobile-android-lifecycle.sh`; `scripts/smoke-mobile-android-remote-browser.sh` | `待分配` | Desktop/Android/Mobile `LocalBackend` 与 `RemoteBrowser` 模式选择、URL 验证、本地 loopback/embedded 后端装配入口；typed bundled-local bootstrap 与 local-only Tauri ACL/plugin 是 backend preference bridge 的唯一 capability source，RemoteBrowser 以 native external `WindowConfig` 直接创建且普通浏览器不注册 facade/Tauri commands；Desktop native menu/tray coordinator 与 Mobile Android/iOS platform control 分别独占 remote-to-local lifecycle，Mobile 控件只在 preference-driven RemoteBrowser 出现；recovery transition guard 只阻止最后窗口提前退出，typed diagnostic 只对 bundled-local command 可见，远端 WebView 退休后才注册 local-only IPC，失败必须清理候选 runtime 或重启；Mobile supervisor 独占一个 process-scoped EmbeddedServerRuntime 与 single-flight、可取消的 transport/session generation，RemoteBrowser cold start 不创建 embedded runtime；Android API 29+/WebView 137+ 是 writable receipt 支持基线，真实 non-extractable Ed25519 probe 仍是写闸门，缺失能力保持只读；Desktop 向 sidecar二选一传递 host 验证后的 canonical Git executable 或 unavailable marker，不传完整 PATH；shell 不直接写 authority。 |
| `acceptance_evidence_runtime` | `部分承载` | `tools/baseline/src/acceptance_matrix/producer/`; `tools/baseline/src/acceptance_matrix/receipt.rs`; `tools/baseline/src/acceptance_matrix/execution_group.rs`; `tools/baseline/src/acceptance_matrix/tag_ready.rs`; `tools/baseline/src/release_candidate/`; `tools/baseline/src/release_version_order.rs`; `docs/registry/acceptance-producers.json`; `.github/workflows/release-candidate.yml`; `.github/workflows/acceptance-aggregate.yml`; `.github/workflows/release.yml` | `待分配` | Rust CLI 独占 acceptance producer 选择、依赖拓扑、预检、串行执行、超时、producer/execution 绑定、schema 3 receipt、claims 绑定、root-pinned/resource-bounded 聚合、candidate allowlist/hash/SPDX policy 与 tag-ready 判定；CI 实际执行已绑定 test/script producer，candidate workflow 生成 exact-HEAD Docker/Desktop/Android/RemoteBrowser receipts 与 sealed bytes，aggregate 只接受显式 immutable run，tag workflow 只 promotion；Node/PowerShell 只承载 Docker、浏览器、ADB、WebView、installer 与签名工具等宿主动作，不拥有 evidence freshness 或发布 authority。仍有 document-only case 与 conditional iOS receipt 迁移工作，因此保持部分承载。 |

## Notes

- `source_control_runtime -> Git main mirror bridge` 仍以 `docs/plan/05_diff_logic.md#git-mirror-lifecycle` 为权威边界；NoteGit/ngit 是唯一 Source Control authority，Git main mirror 不再由 `source_control.git_bridge` 配置切换；`crates/core/src/git_bridge/git_cmd.rs` 只消费可选的 trusted executable process binding，不改变该 authority。
- Route B 已批准：`remote_projection_transport_runtime` 只拥有 projection push 与 remote source acquisition；immutable session/review/apply 归 `remote_import_runtime`，前端归独立 `remote_import_client`。B4 已删除 pull→workspace→External Changes 路径，不得以兼容、诊断或 fallback 名义恢复。
- `session_client -> scope_client -> document_client -> pending_overlay_runtime -> write_confirmation_runtime` 是 Web write confirmation 主链；`pending_overlay_runtime` / `write_confirmation_runtime` 已有 typed contract，头段现在重启物理归带，旧 DEFER 裁定不再作为实现目标。
- `document_runtime -> render_projection_runtime -> widget_bridge_runtime / outline_projection_runtime` 必须保持 projection-only 边界。
- `ui_shell -> application_control -> feature_runtime` 当前是抽象分层，不应为了满足表格而创建空模块。
- cli server 结构中 `server/runtime/` 带已重启实施：startup assembly 必须收敛为 runtime parts；`start_server_with_bound_listener` 是当前已绑定 listener 的 composition root，不能重新引入 `start_server_with_options` 作为长期承载边界；`repo_scope_sync_runtime` / `session_runtime` / `auth_gateway` 仍可按关切分离。
- crates/core 顶层 `authority/`/`projection/`/`scope/` 重组经 §8 核对 **DEFER**:蓝图「projection 与 authority 必须明确分层」的实质要求仍以专用 runtime module 承载，不为目录指标强拆。`watcher_runtime` 的剩余 blocker 是 W8 repo-local isolation、W9 Windows overflow 与 W10 同 HEAD receipts；A1/B1 W7 skeleton 与 C1′ host-local alias 已接入，ownership-aware RemoveLocalRepo 的独立 authority/cleanup drift 登记在各 owner row，不得伪装成 watcher 自身未实现的 lifecycle。
