<!-- Generated: 2026-07-07 | Updated: 2026-07-22 -->

# First-Tag Format Matrix

This registry is the approved first-tag target index. It does not introduce new
engineering rules; authoritative behavior remains in `docs/plan/`.

## Matrix

| Surface | Authority | Approved first-tag target | Current implementation state | Code owner | Failure / reset / repair posture |
|---|---|---|---|---|---|
| Ledger entry payload | `docs/plan/03_storage/authority.md#ledger-entry-format-contract` | `LEDGER_ENTRY_FORMAT_VERSION = 3`; magic `DEVELDG3`; project-owned postcard payload | 已对齐：代码为 v3 / `DEVELDG3`。 | `crates/core/src/models/ledger_decode.rs` | Missing magic, missing version, old codec payload, or unsupported version fail closed. Explicit offline `--allow-legacy-v2` export may read `DEVELDG2` without admitting it to normal runtime. |
| Redb repo schema gate | `docs/plan/03_storage/authority.md#redb-schema-version-contract`; `docs/plan/03_storage/authority.md#remote-import-workflow-tables`; `docs/plan/03_storage/authority.md#projection-fault-recovery-table` | `REDB_SCHEMA_VERSION = 4`，local-authority profile 必须包含 Remote Import session/runtime tables 与 repo-local `PROJECTION_FAULTS`；v3 或缺 required table 的开发 DB 直接 fail closed，不自动迁移、不保留 adapter | 已对齐：v4、UUID-first filename、Remote Import workflow tables、required Projection Fault table、shadow exclusion、缺表 fail-closed 与 B4 Pending rematerialization orchestration 均已落地。 | `crates/core/src/ledger/schema.rs`; `crates/core/src/remote_import/`; `crates/core/src/projection_fault/` | Missing/mismatched top-level schema version 或缺任一 local required table 都在 normal repo/query entry 前 fail closed。Remote shadow 不创建 workflow/fault tables。旧开发数据只能由旧 HEAD 导出后重建；`--allow-legacy-v2` 仅是 Ledger 离线救援，不是 Redb v3/v4 adapter。 |
| WebSocket binary protocol | `docs/plan/07_network.md#server-ws-runtime`; `docs/plan/07_network.md#remote-import-wire-contract`; `docs/plan/07_network.md#repo-control-wire-contract` | `WS_PROTOCOL_VERSION = 5;`; `MIN_SUPPORTED_WS_PROTOCOL_VERSION = 5;`; magic `DEVEWSF4`; nested typed Remote Import + Repo Control alias/create + removal Prepare/Execute、optional opaque fallback binding与single typed scope finalization；删除无版本 JSON/legacy fallback | 部分对齐：当前代码已切换F4/v5并实现R3 admission、R4 quarantined settlement及R5 single typed finalization/Web/CLI；R6 fresh跨平台证据尚未封存。无v4 adapter。 | `crates/core/src/protocol/frame.rs`; `crates/core/src/protocol/remote_import.rs`; `crates/core/src/protocol/repo_control.rs` | Missing/wrong binary magic or unsupported protocol version fails closed with structured protocol error。F4/v1/v2/v3/v4 无 adapter；只保留显式带当前版本的 debug JSON。Removal success不得由`SC_REPO_NOT_SELECTED`或两帧顺序推断。 |
| Projection Locator / host alias | `docs/plan/03_storage/projection.md#projection-locator-contract`; `docs/plan/04_repository.md#host-repo-alias-contract` | `ledger/.host/projection-locators.toml` stores immutable `RepoId -> (projection_base, workspace_segment)`；独立 alias store owns `RepoId -> alias/revision` and deterministic JSON v1；peer payload never carries alias | 部分对齐：locator v2、alias store、JSON v1、F4/v5 CAS、exact RepoList、thin client及R4 membership-revoked conditional cleanup已实现；exact owner-plan drift repair代码已实现；R6 fresh证据仍未完成。 | `crates/core/src/ledger/manager/projection_locator.rs`; `crates/core/src/ledger/manager/host_repo_alias.rs`; `apps/web/src/runtime/repo_control_client.rs` | Locator identity/path conflict enters `DegradedLocator`。Alias import unknown/invalid/duplicate entries warning + skip；accepted batch atomic，store failure global。Removal cleanup preserves unrelated RepoId rows and fails closed on revision drift. |
| Repo catalog membership / lifecycle receipt | `docs/plan/04_repository.md#local-repo-removal-contract`; `docs/plan/04_repository.md#repo-lifecycle-coordinator`; `docs/plan/03_storage/index.md` | Per-RepoId deterministic JSON v2 catalog record is the sole normal-membership cut；host-local removal preparation record is deterministic JSON v4；`CutAttempted`/`CutObserved`、owner checkpoints、short-lived repair authorization与`TerminalCandidate`覆盖committed-or-unknown debt；removal uses issuer-bound hashed five-minute one-time token、optional opaque fallback与single typed finalization；persistent authority lock pathname never enters cleanup；same-RepoId readmission uses owner-prepared `ReopeningPrepared` + fresh Normal commit + pure guarded activation；none enters Ledger/sync | 部分对齐：R3 admission/token、R4 O1-FREEZE/quarantine/two-phase settlement及R5 single typed finalization/Web/normal offline-proxy CLI已实现。R6将record切换至v4并补齐exact-plan repair与0/20/21/22 process outcome；旧catalog v1/removal record v1-v3无adapter。Option A identity v2、owner-prepared reincarnation和唯一production coordinator路径已实现；fresh跨平台/浏览器证据仍未完成。 | `crates/core/src/ledger/manager/authority_storage_runtime/`; `crates/core/src/ledger/manager/repo_catalog_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_job_runtime.rs`; `apps/cli/src/server/runtime/repo_lifecycle_runtime.rs`; `apps/cli/src/server/local_repo_removal_cli_runtime.rs`; `apps/web/src/runtime/repo_control_client.rs` | Missing/mismatched record fails closed。Pre-cut removal requires exact membership-bound token/manifest and atomic admission；pre-cut failure exact-compensates or remains readonly/repair；post-cut failure retains exact tombstone/repair debt and never guesses rollback or restores membership。Repair token只重试原owner identity，不重绑定replacement；Reincarnation lock missing/replaced或post-cut identity drift不得create lock或删除fresh incarnation。 |
| Remote Projection transport / Remote Import | `docs/plan/06_backup.md#remote-projection-transport-contract`; `docs/plan/06_backup.md#remote-import-session-contract`; `docs/plan/06_backup.md#remote-import-removal-owner-plan` | `projection-remote <provider> push` + immutable source acquisition → Remote Import session/review/sealed Ledger Apply；host-local profile binding，不暴露 secret/path/digest；removal按O1-FREEZE先quiesce provider并封存owner plan，pre-cut invalidation仅是逻辑状态，cut后whole-root quarantine/delete | 部分对齐：Remote Import主体及R4 provider-quiesced plan、logical invalidation和whole-root quarantine/delete已落地；removal owner-plan exact drift repair代码已实现；B5 typed client与B6/R6 fresh receipts仍阻塞tag。 | `crates/core/src/remote_projection/`; `crates/core/src/remote_import/`; `crates/core/src/ledger/manager/prepared_change_batch/`; `apps/cli/src/remote_projection_transport/`; `apps/cli/src/remote_import_runtime.rs`; `apps/cli/src/commands/projection_remote.rs`; `apps/cli/src/commands/remote_import/`; `apps/cli/src/server/handlers/remote_import/` | Locator/profile/provider/path/budget failure must precede authority effects。Prepare 只封存 immutable blobs；任意 blocker 禁用 whole-session Apply；authority transaction 持久化 Pending receipt，post-commit writeback 后单调收敛为 Written/Degraded，不回滚或重复写 Ledger。Removal不得让coordinator直接删除capture或在Removed cut后改写session row。 |

## Release Gate Binding

`cargo run -p deve_baseline -- release` pins this registry, the plan constants,
and the code constants above. A format change must update `docs/plan/` first,
then this registry and the matching baseline spec before code can claim first-tag
readiness.

Implementation status after approved R0 target: Ledger payload v3、Redb schema v4、Remote Import
backend/CLI product wire、Mounted admission、post-commit writeback、immutable Projection
Locator、host-local alias、F4/v5 Repo Control admission、zero-repo host、R4 ownership-aware lifecycle settlement、
R5 single typed finalization/thin Web/normal offline-proxy CLI 与per-RepoId authority ownership已对齐；
repair-specific child-process 20/21/22、candidate browser completeness与R6 fresh跨平台证据仍是显式blocker；旧
pull/continuation/External Changes bridge 与 name-only repo control 均已删除。B5 Remote
Import client、B6 与 first-tag freshness evidence remain explicit blockers.
