# 00_engineering_constitution.md - 工程宪法

## Metadata

- `Layer`: Foundation
- `Status`: Governing Rule
- `Scope`: architecture, plan, operation, acceptance, and implementation work
- `Counterpart Overview`: `docs/overview/architecture.md`

本宪法建立在管理员 / USER 的骨架级判断具备高质量事实、推理与利弊分析的前提下。它的目标不是弥补低质量判断，而是约束工程实现忠实执行已批准判断，避免系统背离本体、骨架被局部功能污染、结构性失稳累积。

## 1. 总纲

1. 必须先定义总骨架与边界，再定义模块，再定义实现。
2. 局部功能只能填入既定骨架，不得反向塑造骨架。
3. 系统优先保持整体结构长期稳定，而不优先追求局部极致最优。
4. 系统允许模块替换与实现迭代，但不允许日常功能迭代以侵蚀方式改写主骨架。

## 2. 设计顺序

任一设计任务必须按以下顺序执行：

1. 定义总骨架与边界
2. 列出模块清单
3. 细化实现逻辑

禁止跳过前项直接进入后项。探索性实现可以存在，但不得伪装为正式骨架实现，也不得未经收敛直接沉淀为长期主干。

## 3. 优先级

当目标冲突时，严格按以下顺序取舍：

1. 正确性 / 功能实现
2. 可用性 / 使用体验
3. 根基兼容性
4. 可维护性 / 可诊断性
5. 性能
6. 内存占用
7. 外存占用
8. 其它次级因素

低优先级目标不得破坏高优先级目标。根基兼容性仅指 CA、网络协议、时区、Git 生态、编程语言生态等长期稳定且被广泛接受的底层外部根基。

## 4. 骨架构造

- 对长期稳定、基础性强、行业共识强、自建收益低且维护成本高的根基对象，优先兼容。
- 对尚未形成长期生态、实现简单、替代成本低的能力，优先内嵌、重构或借鉴后纳入自身系统。
- 外部环境依赖必须通过兼容层或适配层进入系统，核心逻辑不得直接绑定环境细节。
- 抽象只能建立在长期稳定的本体对象、关系与能力边界上，不得建立在首版实现、具体格式或局部技术细节上。
- 同一变化族的具体实现必须位于同一抽象下，并以平级模块存在。
- 插件机制不是架构起点，而是平级实现数量与替换需求增长后的自然承载形式。

## 5. 工程蓝图最低项

一个可施工工程蓝图至少必须明确模块职责、调用关系、输入输出、状态变化、错误处理、配置入口、生命周期、扩展点、替换点与性能关键路径。

只能说明大致分层、模块大致存在、目标大致正确，而不能继续下钻到调用逻辑与运行过程的方案，只是概念草图，不构成工程蓝图。

## 6. 默认架构模型

本项目使用四层 canonical call architecture：

1. `User Operation`
2. `Instruction Interface`
3. `Flow Coordination`
4. `Execution Domain`

`Object Plane` 是侧边对象层，用于表达 `doc::content`、`confirmed_op`、`repo::scope`、`config::runtime` 等被调用链最终触达的对象。它不是第五个主调用层。

此前“五层架构”表述与当前四层图没有本质冲突：第五层对象层在当前总蓝图中显式收敛为 `Object Plane`，以避免把对象平面误读成调用步骤。`Ownership Axis` 也不是主层级，只表达长期责任归属。

## 7. 一致性与可靠性

- 新增功能只能通过既定边界接入，不允许跨层直连、跨模块写入或绕过统一入口。
- 对任何核心对象、核心状态与核心配置，必须明确唯一可信来源。
- 核心流程必须先定义失败路径：失败条件、传播方式、回退策略、重试边界、用户可见结果。
- 日志、错误码、状态追踪、关键事件记录与性能指标必须作为架构内建能力进入骨架。
- 核心接口、数据模型、配置模型变化时，必须回答旧版本兼容、迁移、失败回退与验证。
- 可重复触发的核心流程必须具备幂等性或显式去重机制。
- 并发读写核心状态时，必须明确谁先、谁后、谁覆盖谁、冲突如何解决。

## 8. 变更治理

任何方向、能力轴、抽象、边界划分或骨架判断，一旦经分析被认定为错误、失效或已被证伪，就必须立即进入骨架纠偏流程。这里的“立即”指立即启动分析、审查与决策流程，而不是绕过审查直接改动主骨架。

任何骨架级变更必须先提交分析报告，并由管理员 / USER 明确批准。报告至少包含变更原因、当前问题或证伪点、收益、风险与代价、对模块和模型的影响、迁移兼容回退验证、以及不变更的后果。

管理员 / USER 的骨架级判断也必须接受高质量反证。只要存在充分理由认为判断可能失误、证据不足或忽略关键代价，就必须提交复核分析报告。

## 9. AI 执行规则

AI 处理本项目时必须以本宪法为最高工程治理约束之一。若局部任务、旧 report、当前代码习惯与本宪法冲突，默认先停在分析层说明冲突，再按管理员 / USER 批准后的路径执行。
