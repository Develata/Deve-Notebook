# 11_i18n.md - 语言篇 (Internationalization)

## Metadata

- `Layer`: `Application / UI Shell`
- `Status`: `Current MUST`
- `Counterpart Feature`: `docs/features/11_i18n.md`
- `Counterpart Acceptance`: `docs/acceptance-cases/09_i18n.md`
- `Primary Code Areas`: `apps/web/src/i18n/`, `apps/web/src/components/` (t::* facade consumers)

## 1. Internationalization Strategy (国际化策略) {#i18n-facade-contract}

*   **技术选型**：使用轻量级 `crate::i18n::{Locale, t::*}` facade。
    *   **优势**：零代码生成、低依赖、调用面稳定，适合 768 MB VPS 约束下的前端体积控制。
    *   **实现约束**：根组件初始化 `Locale` 上下文，各组件统一通过 `t::xxx::yyy()` 读取文案。
*   **UI 文本管理**：
    *   核心 UI 文本 **MUST** 全部通过 `t::xxx::yyy()` facade 进行管理，严禁新增硬编码字符串。
*   **Format Handling (格式处理)**:
    *   **Date/Time/Number**: 严禁手动拼装 (e.g., `YYYY-MM-DD`)。**MUST** 使用 `Intl` API (JavaScript) 或 Rust 的 `time` crate 进行本地化格式输出。
*   **Backend Error Handling (后端错误处理)**:
    *   后端 API **MUST NOT** 返回自然语言错误信息 (e.g., "密码错误")。
    *   **MUST** 返回标准错误码 (e.g., `AUTH_INVALID_PASSWORD`)。
    *   前端根据 Error Code 查找 `i18n` 映射表进行展示。
    *   本章只定义错误码目录与前端映射规则；传输层/协议层约束应写入网络或薄客户端账本文档。

## 2. Resource Management (语言资源管理) {#i18n-resource-management}

*   **Structure**: 语言资源默认以 Rust 模块函数组织；若后续迁移到外部资源文件，必须保持 `t::xxx::yyy()` 调用面兼容。
*   **Fallback**: Rust `t::*` facade 对已支持 Locale 的 Key 覆盖由编译期保证；不支持的 Locale 标签 **MUST** 在 locale detection/selection 阶段回退到 `en-US`。若后续迁移为外部资源文件并允许运行期缺 Key，则缺失 Key **MUST** 自动回退到 `en-US`。
*   **Detection**: 启动时优先级:
    1.  User Config (`locale` setting).
    2.  Browser Preference (`navigator.language`).
    3.  Default (`en-US`).


## 3. Related Commands (本章相关命令)

*   `Global: Toggle Language`: 按顺序在支持的语言列表 (e.g., en -> zh -> ja -> en) 中循环切换界面语言。

## 4. Related Configuration (本章相关配置)

*   `locale`: 界面语言设置 (e.g., `en-US`, `zh-CN`).

## 5. I18n Keys Reference {#i18n-keys-reference}

### Source Control (`t::source_control::xxx`)

| Key              | En             | Zh         |
| ---------------- | -------------- | ---------- |
| `title`          | Source Control | 源代码管理 |
| `repositories`   | Repositories   | 存储库     |
| `changes`        | Changes        | 更改       |
| `staged_changes` | Staged Changes | 暂存的更改 |
| `history`        | History        | 历史记录   |
| `graph`          | Graph          | 图形       |

### Common (`t::common::xxx`)

| Key        | En       | Zh       |
| ---------- | -------- | -------- |
| `create`   | Create   | 创建     |
| `new_file` | New File | 新建文件 |

### Header (`t::header::xxx`)

| Key        | En              | Zh       |
| ---------- | --------------- | -------- |
| `settings` | Settings        | 设置     |
| `home`     | Home            | 首页     |
| `open`     | Open Index      | 打开目录 |
| `command`  | Command Palette | 命令面板 |

### Sidebar (`t::sidebar::xxx`)

| Key       | En                 | Zh       |
| --------- | ------------------ | -------- |
| `no_docs` | No documents found | 暂无文档 |

### Settings (`t::settings::xxx`)

| Key           | En                                 | Zh                       |
| ------------- | ---------------------------------- | ------------------------ |
| `title`       | Settings                           | 设置                     |
| `close`       | Close                              | 关闭                     |
| `about`       | About                              | 关于                     |
| `version`     | Version                            | 版本                     |
| `language`    | Language                           | 语言                     |
| `hybrid_mode` | Hybrid Editing                     | 混合编辑                 |
| `hybrid_desc` | Hide Markdown syntax while reading | 阅读时隐藏 Markdown 语法 |
| `coming_soon` | Future setting: unavailable in this capability profile | 未来设置：当前能力档不可用 |

### Bottom Bar (`t::bottom_bar::xxx`)

| Key       | En         | Zh        |
| --------- | ---------- | --------- |
| `words`   | Words      | 字数      |
| `lines`   | Lines      | 行数      |
| `col`     | Col        | 列        |
| `ready`   | Ready      | 就绪      |
| `syncing` | Syncing... | 同步中... |
| `offline` | Offline    | 离线      |

### Playback (`t::playback::xxx`)

| Key     | En       | Zh       |
| ------- | -------- | -------- |
| `label` | PLAYBACK | 回放控制 |

### Command Palette (`t::command_palette::xxx`)

| Key               | En                     | Zh                |
| ----------------- | ---------------------- | ----------------- |
| `placeholder`     | Type a command...      | 输入命令...       |
| `no_results`      | No results found.      | 未找到结果。      |
| `open_settings`   | Open Settings (config) | 打开设置 (config) |
| `toggle_language` | Toggle Language        | 切换语言          |

### Search (`t::search::xxx`)

| Key                   | En                   | Zh                   |
| --------------------- | -------------------- | -------------------- |
| `placeholder_command` | Search commands...   | 搜索命令...          |
| `placeholder_branch`  | Switch branch...     | 切换分支...          |
| `placeholder_file`    | folder/.../file(.md) | 文件夹/.../文件(.md) |

## 6. Error Code Catalog (错误码目录) {#i18n-error-code-catalog}

本节是错误码清单唯一权威。其他章节 **MUST** 引用本节，不得另建平行错误码目录。

后端 API 返回的标准错误码。前端根据 Error Code 查找 i18n 映射表进行展示。
如需补充调试信息，后端 **MAY** 返回 `detail` 字段；但前端展示与分支判断 **MUST** 仅依赖 `code`，不得依赖自然语言 `detail`。

### Common Errors
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `REQUEST_FAILED` | 500 | Request failed | 请求失败 |

### Authentication Errors (`AUTH_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `AUTH_INVALID_PASSWORD` | 401 | Invalid password | 密码错误 |
| `AUTH_TOKEN_EXPIRED` | 401 | Token expired | 凭证已过期 |
| `AUTH_TOKEN_MISSING` | 401 | Authentication required | 需要登录 |
| `AUTH_RATE_LIMITED` | 429 | Too many attempts | 请求频率超限 |
| `AUTH_CSRF_MISMATCH` | 403 | Request rejected | 请求被拒绝 |

### Storage Errors (`STORAGE_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `STORAGE_DB_LOCKED` | 503 | Database is locked | 数据库被锁定 |
| `STORAGE_NOT_FOUND` | 404 | Document not found | 文档不存在 |
| `STORAGE_CONFLICT` | 409 | Write conflict | 写入冲突 |
| `STORAGE_PERSIST_FAILED` | 500 | Failed to persist change | 变更持久化失败 |

### Source Control Errors (`SC_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `SC_REPO_NOT_SELECTED` | 409 | Repository not selected | 当前未选择激活仓库 |
| `SC_REMOTE_BRANCH_READONLY` | 409 | Remote branch is read-only | 远程分支为只读 |
| `SC_REPO_CONTEXT_INVALID` | 409 | Repository context is invalid | 仓库上下文无效 |
| `SC_STALE_SCOPE` | 409 | Repository scope is stale | 仓库作用域已过期 |
| `SC_PENDING_NOT_FOUND` | 409 | Pending change not found | 待处理变更不存在 |
| `SC_STAGED_NOT_FOUND` | 409 | Staged change not found | 暂存变更不存在 |
| `SC_DOC_NOT_FOUND` | 404 | Document not found | 文档不存在 |
| `SC_COMMIT_NOT_FOUND` | 404 | Commit not found | 提交不存在 |
| `SC_NOTHING_TO_COMMIT` | 409 | Nothing to commit | 没有可提交内容 |
| `SC_CONFLICT_TARGET_MISSING` | 409 | Conflict target missing | 冲突目标已失效 |

### Document Errors (`DOC_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `DOC_NOT_FOUND` | 404 | Document not found | 文档不存在 |
| `DOC_CONTEXT_INVALID` | 409 | Document context is invalid | 文档上下文无效 |

### Sync Errors (`SYNC_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `SYNC_EDIT_REJECTED` | 409 | Edit rejected | 编辑被拒绝 |
| `SYNC_REPO_UNBOUND` | 409 | Session is not bound to this repository | 当前会话尚未绑定到该仓库 |
| `SYNC_REPO_ROUTE_MISMATCH` | 409 | Repository route mismatch | 仓库路由不匹配 |
| `SYNC_SNAPSHOT_REQUIRED` | 409 | Snapshot refresh required | 需要刷新快照 |
| `SYNC_INVALID_PAYLOAD` | 400 | Invalid sync payload | 同步载荷无效 |
| `SYNC_PEER_UNAUTHENTICATED` | 403 | Browser peer is not ready for writing | 浏览器写入身份尚未完成认证 |
| `SYNC_PEER_UNKNOWN` | 403 | Unknown peer | 未知节点 |
| `SYNC_VERSION_MISMATCH` | 409 | Protocol mismatch | 协议版本不兼容 |
| `SYNC_DECRYPT_FAILED` | 400 | Decryption failed | 数据解密失败 |
| `SYNC_DISCONNECTED` | 503 | Connection lost | 连接已断开 |

### Plugin Errors (`PLUGIN_*`)
| Code | HTTP Status | En | Zh |
|:---|:---|:---|:---|
| `PLUGIN_INVALID_MESSAGE` | 400 | Invalid plugin host message | 插件宿主消息无效 |
| `PLUGIN_UNSUPPORTED_MESSAGE` | 400 | Unsupported plugin host message | 插件宿主不支持该消息 |
| `PLUGIN_UNKNOWN_PLUGIN` | 404 | Plugin not found | 插件不存在 |
| `PLUGIN_CAPABILITY_DENIED` | 403 | Plugin capability denied | 插件能力未授权 |
| `PLUGIN_RUNTIME_ERROR` | 500 | Plugin runtime error | 插件运行时错误 |
| `PLUGIN_SERIALIZATION_ERROR` | 500 | Plugin result is not serializable | 插件结果不可序列化 |
