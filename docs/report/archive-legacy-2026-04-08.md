# Archive: Legacy Raw Gap Scans - 2026-04-08

Legacy raw scans from 2026-04-08 are retained here as forensic input, not live project status.

This file consolidates retired `docs/report/` entries to keep the active report directory small. The full original reports remain available in git history before this consolidation commit. `docs/plan/` was not modified.

- Retired source reports: 5
- Active guard-referenced reports were intentionally left as standalone files.

| Retired source | Kind | Original title | Key evidence / conclusion |
| --- | --- | --- | --- |
| `docs/report/baseline-2026-04-08.md` | baseline | Baseline Report — 2026-04-08 | > gap. Several P0/P1 items recorded below have since been closed, including sync<br>\\| Auth/JWT/Argon2/Rate limit \\| ✅ **全部到位** + 近期大量 fail-closed 加固 \\| gap-server \\|<br>\\| `server/mod.rs` 168 行 \\| ⚠️ **回归至 251 行**，源于近 20 次 fail-closed 安全加固积累 \\| git log + wc \\| |
| `docs/report/gap-core-2026-04-08.md` | selection/gap-scan | gap-core-2026-04-08 | \\| 04 §硬不变量 \\| 若 `Workspace_r != P_r`，系统 MUST 能通过 `pending_fs_ops_r` / `staging_r` 解释偏差 \\| `crates/core/src/ledger/manager/source_control_query_ops.rs:3-27`<br>`crates/core/src/ledger/manager/source_control_ops.rs:90-132` \\| ⚠️ partial \\| Code models pending/staged deviations and exposes them via `list_changes`, but no explicit drift fault detector was found for the “无法解释则视为状态漂移故障” branch. \\|<br>\\| 04 §硬不变量 \\| 路径与树结构事实 MUST NOT 通过 metadata table 直写；必须先形成 Structure Facts \\| `crates/core/src/ledger/manager/structure_ops.rs:76-95`<br>`crates/core/src/ledger/manager/structure_projection.rs:8-42` \\| ✅ implemented \\| Structure path says “结构事实必须先进入 Ledger，再更新 projection”; projection module folds `StructureOp` into metadata/path caches. \\|<br>\\| 04 §投影与持久化 \\| Ledger Append 成功而投影写入失败时，系统 MUST 记录可恢复故障，并支持从 Ledger 重建 Vault \\| `crates/core/src/sync/projection_io.rs:32-37`<br>`crates/core/src/sync/rebuild_projection.rs:10-51` \\| ⚠️ partial \\| Rebuild support exists; persistence errors clear guard and return `Err`. No recoverable fault record/store was found in scope. \\| |
| `docs/report/gap-i18n-ui-2026-04-08.md` | selection/gap-scan | Gap Scan: i18n + UI Component Structure | Historical evidence summary; see git history for full retired source. |
| `docs/report/gap-server-2026-04-08.md` | selection/gap-scan | gap-server-2026-04-08 | > owner-only permission、login audit `timestamp/user_agent`、CORS wildcard fail-closed 与 dev-only<br>\\| `05_network.md §Main / Proxy` \\| 端口策略：本地开发环境 **MAY** 暴露显式端口用于诊断；生产环境 **MUST** 通过单一配置端点或反向代理提供稳定入口。 \\| [node_role_http.rs](apps/cli/src/server/node_role_http.rs#L8), [mod.rs](apps/cli/src/server/mod.rs#L175) \\| ⚠️ partial \\| Plan: `生产环境 MUST 通过单一配置端点或反向代理提供稳定入口。` Code exposes ports via `Json(serde_json::json!({ "role": r.role, "ws_port": r.ws_port, "main_port": r.main_port, }))` and sets both to the runtime port for main mode. \\|<br>\\| `05_network.md §Deterministic Routing` \\| `SyncHello` **MUST** 提供 `repo_id` 与当前 vector，确保 Server 能决定是走增量同步还是快照回退。 \\| [handlers/sync/hello/mod.rs](apps/cli/src/server/handlers/sync/hello/mod.rs#L14) \\| ✅ implemented \\| `SyncHelloInput` requires `remote_vector` and `repo_id`; `handle()` passes both into `engine.handshake(...)`. \\| |
| `docs/report/gap-web-2026-04-08.md` | selection/gap-scan | gap-web-2026-04-08 | \\| 03 §Interaction Philosophy \\| Cursor Reveal: when cursor touches/enters rendered source range, render layer MUST immediately yield to raw Markdown \\| — \\| ❌ missing \\| No Markdown widget/render-layer component in `apps/web/src/editor/**`; no cursor-range reveal hook found in `apps/web/src/**`. \\|<br>\\| 03 §Phase 2 \\| Higher-priority inline elements MUST NOT render lower-priority elements inside \\| — \\| ❌ missing \\| No general inline Markdown parser/hook in `apps/web/src/editor/**`; only outline/chat-specific helpers exist. \\|<br>\\| 03 §Nested Hell \\| Code blocks inside blockquotes MUST use layered `background-image` composition \\| — \\| ❌ missing \\| No corresponding component/hook in `apps/web/src/**`; implementation was specified in CSS outside this scope. \\| |
