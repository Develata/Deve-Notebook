# Native Target-host Evidence - <target> - YYYY-MM-DD

Target: Desktop macOS | Desktop Windows | Mobile iOS

Workflow run: <GitHub Actions URL or N/A for local target-host execution>

Host OS: <host OS, version, architecture>

Tool versions:

- rustc: <version>
- cargo tauri: <version>
- node: <version>
- npm: <version>
- platform toolchain: <xcodebuild / MSVC / WiX / NSIS / iOS toolchain state>

Commands:

```bash
<exact commands executed>
```

Command results:

- <command or workflow step>: <success, failure, skipped, or explicit N/A>

Artifact paths:

- <package artifact path, workflow artifact name, or explicit N/A with reason>

Install result: <passed, failed, or explicit N/A with reason>

Startup result: <passed, failed, or explicit N/A with reason>

Process runtime boundary: default no-Tauri closed; Desktop LocalBackend controlled child-process; Mobile child-process closed

Native authority writes: closed

Conclusion: <ready, blocked, diagnostic-only, or failed with reason>
