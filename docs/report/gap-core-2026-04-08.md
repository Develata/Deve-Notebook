> Superseded note (2026-04-28): 本文件是旧 core gap raw scan，只保留作 forensic input，
> 不再作为 active TODO。Watcher runtime、sync `known_vector/server_vector`、browser degraded
> storage gating、path normalization cleanup、graph read-only projection 与多项 source-control identity
> hardening 已由后续代码和 `code-review-2026-04-28.md` 覆盖。下表中的相反断言按历史快照理解。

| Plan §Section | Clause (one line) | Code Location | Status | Evidence |
|---|---|---|---|---|
| 04 §三库隔离 | Dirty Workspace 偏差 MUST 由 repo-scoped `pending_fs_ops` / `staging` 显式跟踪 | `crates/core/src/ledger/schema.rs:61-68`<br>`crates/core/src/ledger/manager/source_control_ops.rs:88-132`<br>`crates/core/src/ledger/manager/source_control_query_ops.rs:3-27` | ✅ implemented | Tables `PENDING_FS_OPS`, `PENDING_FS_DOC_INDEX`, `STAGED_DOC_INDEX` exist; `stage_pending` moves pending to staging; query invariant says worktree diff is `pending_fs_ops + staging`, not a second truth source. |
| 04 §分支存储结构 | `.redb` 文件名 MUST 是人类可读的 `repo_name.redb` | `crates/core/src/ledger/init.rs:82-87`<br>`crates/core/src/ledger/shadow/management.rs:37-40`<br>`crates/core/src/ledger/manager/remote_repo_allocate.rs:14-21` | ⚠️ partial | Plan: `文件名 MUST 是人类可读的 repo_name.redb`.<br>Code local path is `format!(\"{}.redb\", final_name)` in `init.rs:86`; remote runtime path in `shadow/management.rs:39` is `format!(\"{}.redb\", repo_id)`, while allocator also has readable naming in `remote_repo_allocate.rs:14-21`. |
| 04 §分支存储结构 | `.notegit/` MUST 被 Watcher 忽略 | `crates/core/src/sync/scan.rs:29-39`<br>`crates/core/src/sync/rebuild_projection.rs:67-72` | ⚠️ partial | Scan skips internal repo paths via `is_internal_repo_path`; projection rebuild explicitly skips `.notegit`. No real-time watcher implementation was found in scope (`rg "notify|RecommendedWatcher|watch\\("` under scope returned no runtime hit). |
| 04 §分支存储结构 | `.notegit/` MUST NOT 被跨 Repo 复用 | `crates/core/src/ledger/manager/workspace.rs:29-40` | ❌ missing | Code derives repo-scoped `.notegit/` roots, but no enforcement against cross-repo reuse was found in scope. |
| 04 §唯一权威模型 | 权威状态 MUST 定义为单一有序账本事实日志 `L_r` | `crates/core/src/ledger/schema.rs:22-26`<br>`crates/core/src/ledger/ops_write_direct.rs:7-35`<br>`crates/core/src/ledger/ops_write_generated.rs:37-88` | ✅ implemented | Single ordered log table `LEDGER_OPS`; both append paths compute `last_seq + 1` and insert serialized `LedgerEntry` into `LEDGER_OPS`. |
| 04 §唯一权威模型 | `pending_fs_ops_r` / `staging_r` MUST NOT 升格为权威真相 | `crates/core/src/ledger/schema.rs:61-68`<br>`crates/core/src/ledger/manager/source_control_query_ops.rs:3-27`<br>`crates/core/src/sync/handler.rs:3-6` | ✅ implemented | Pending/staging live in separate tables; query invariant says diffs come from `pending_fs_ops + staging`; fs handler invariant says external events only update pending side tables and must not rewrite authoritative ledger/path mapping. |
| 04 §账本事实分层 | 权威事实 MUST 至少分为 Content Facts 与 Structure Facts | `crates/core/src/ledger/manager/commit_ops.rs:20-25`<br>`crates/core/src/ledger/manager/commit_apply.rs:14-29`<br>`crates/core/src/ledger/manager/structure_ops.rs:10-97` | ✅ implemented | Commit flow comment explicitly splits Added/Modified into structure facts then content facts; delete path uses structure delete; structure ops are represented by `StructureOp`. |
| 04 §账本事实分层 | 业务层 MUST NOT 把 `metadata::rename_doc / set_doc_path / delete_doc` 当作主写路径 | `crates/core/src/ledger/manager/metadata_ops.rs:1-5`<br>`crates/core/src/ledger/manager/metadata_repair_ops.rs:3-5`<br>`crates/core/src/ledger/manager/structure_projection.rs:8-11` | ✅ implemented | `metadata_ops.rs` says direct mapping writes are split to repair/rebuild; `metadata_repair_ops.rs` says only repair/migration/rebuild may use them; grep for `metadata::rename_doc|set_doc_path|delete_doc` in scope only hit repair/projection cleanup/projection folding code. |
| 04 §硬不变量 | `Snapshot` / `Vault Projection` / `Path Mapping` / `Commit View` MUST 由 `L_r` 或锚定到 `L_r` 的辅助表唯一导出 | `crates/core/src/sync/rebuild_projection_state.rs:11-32`<br>`crates/core/src/sync/projection_io.rs:6-58`<br>`crates/core/src/ledger/manager/commit_ops.rs:61-65` | ⚠️ partial | Projection rebuild is driven from `LEDGER_OPS` structure facts; projection persistence rebuilds content from ledger. Snapshot saving in `projection_io.rs:46-51` and commit creation in `commit_ops.rs:61-65` are anchored to current ledger seq, but snapshots are also persisted from rebuilt disk content rather than exclusively re-derived on read. |
| 04 §硬不变量 | 同一 Repo 内所有落账本写入 MUST 在追加前被线性化，并由 `GlobalSeq` 决定落盘顺序 | `crates/core/src/ledger/ops_write_direct.rs:10-18`<br>`crates/core/src/ledger/ops_write_generated.rs:60-68` | ✅ implemented | Both append paths derive `new_seq` / `new_global_seq` from `ops.last() + 1` inside one write transaction and then commit. |
| 04 §硬不变量 | 若 `Workspace_r != P_r`，系统 MUST 能通过 `pending_fs_ops_r` / `staging_r` 解释偏差 | `crates/core/src/ledger/manager/source_control_query_ops.rs:3-27`<br>`crates/core/src/ledger/manager/source_control_ops.rs:90-132` | ⚠️ partial | Code models pending/staged deviations and exposes them via `list_changes`, but no explicit drift fault detector was found for the “无法解释则视为状态漂移故障” branch. |
| 04 §硬不变量 | 路径与树结构事实 MUST NOT 通过 metadata table 直写；必须先形成 Structure Facts | `crates/core/src/ledger/manager/structure_ops.rs:76-95`<br>`crates/core/src/ledger/manager/structure_projection.rs:8-42` | ✅ implemented | Structure path says “结构事实必须先进入 Ledger，再更新 projection”; projection module folds `StructureOp` into metadata/path caches. |
| 04 §账本优先写路径 | 内置编辑路径 MUST NOT 先改 Vault 再回填 Ledger | `crates/core/src/plugin/runtime/host/note.rs:56-68`<br>`crates/core/src/sync/mod.rs:168-210` | ⚠️ partial | Controlled write paths found in scope append ledger facts first (`append_patch_in_local_repo` / `append_generated_op_in_local_repo`) and then persist projection. No dedicated “editor mode” boundary was found in scope. |
| 04 §外部编辑摄取 | Watcher 检测到的 `Create / Modify / Delete / Rename` MUST NOT 直接生成 Ledger Append | `crates/core/src/sync/handler.rs:3-6`<br>`crates/core/src/sync/scan_file.rs:3-6`<br>`crates/core/src/sync/pending_rename.rs:3-6` | ✅ implemented | Handler invariant: external file events only update pending side table; scan invariant repeats rename candidates must not rewrite projection truth; pending rename keeps only pending delete/add candidates. |
| 04 §投影与持久化 | 所有系统自发写盘路径 MUST 满足 `Intent -> Ledger Facts -> Snapshot / Tree Projection -> Vault` | `crates/core/src/sync/mod.rs:185-210`<br>`crates/core/src/sync/projection_io.rs:21-40`<br>`crates/core/src/ledger/manager/commit_apply.rs:14-30` | ✅ implemented | Local apply path is append op, then `persist_doc_in_local_repo`; commit path generates structure/content facts then persists projection. |
| 04 §投影与持久化 | `metadata` / `path mapping` / `tree cache` / `NodeMeta` MUST 由 projection builder 写入 | `crates/core/src/ledger/manager/structure_projection.rs:8-42`<br>`crates/core/src/sync/rebuild_projection_state.rs:11-32` | ✅ implemented | Projection folding is centralized in `structure_projection::apply`; rebuild resets projection tables then replays structure facts. |
| 04 §投影与持久化 | Ledger Append 成功而投影写入失败时，系统 MUST 记录可恢复故障，并支持从 Ledger 重建 Vault | `crates/core/src/sync/projection_io.rs:32-37`<br>`crates/core/src/sync/rebuild_projection.rs:10-51` | ⚠️ partial | Rebuild support exists; persistence errors clear guard and return `Err`. No recoverable fault record/store was found in scope. |
| 04 §纯净文件策略 | 系统 MUST 使用 `NodeId` 作为内部追踪标识 | `crates/core/src/ledger/node_meta/core.rs:11-43`<br>`crates/core/src/ledger/manager/structure_ops.rs:37-74` | ✅ implemented | Node projection tables are keyed by `NodeId`; directory structure APIs return/use `NodeId`; file nodes use `NodeId::from_doc_id(doc_id)`. |
| 04 §纯净文件策略 | `NodeId <-> Path/Inode` 映射 MUST 存储在 Store B 专用 Table/Bucket 中 | `crates/core/src/ledger/schema.rs:13-20`<br>`crates/core/src/ledger/init.rs:212-229`<br>`crates/core/src/ledger/inode_index.rs:1-28` | ✅ implemented | `NODEID_TO_META`, `PATH_TO_NODEID`, `INODE_TO_NODEID` are Redb tables initialized in local repo DB; `inode_index.rs` only uses DB tables. |
| 04 §纯净文件策略 | 系统 MUST NOT 向用户 Markdown 注入元数据 | `crates/core/src/plugin/runtime/host/note.rs:56-68`<br>`crates/core/src/sync/projection_io.rs:27-40` | ✅ implemented | Managed note writes compute a reconcile patch from user-provided content, then persist reconstructed content; no frontmatter/UUID injection code was found in scope (`rg "frontmatter|yaml"` returned no hit). |
| 04 §纯净文件策略 | Frontmatter 即使存在，系统也 MUST 视其为普通文本 payload | `crates/core/src/plugin/runtime/host/note.rs:47-67`<br>`crates/core/src/sync/rebuild.rs` | ✅ implemented | In scope, note reads reconstruct raw content from ops and writes reconcile raw text; no frontmatter parser or metadata extractor was found under scope. |
| 04 §纯净文件策略 | Frontmatter 修改 MUST NOT 反向影响 Ledger 系统元数据 | `crates/core/src/plugin/runtime/host/note.rs:56-68`<br>`crates/core/src/ledger/manager/metadata_ops.rs:44-56` | ✅ implemented | Content writes only append text patches; tracked identity lookup remains via node/doc projection, not document body parsing. |
| 04 §映射与监听 | 系统核心 MUST 运行文件系统监听服务，实时监控 Vault | `crates/core/src/sync/watcher/mod.rs:37-117`<br>`crates/core/src/sync/watcher/backend/notify_impl.rs:15-79`<br>`crates/core/src/sync/watcher/dispatch.rs:16-117`<br>`crates/core/tests/watcher_multi_repo_isolation.rs:11-23` | ✅ implemented | Core now starts one fail-closed watcher per repo, performs pre-arm scan, normalizes debounced notify events through a backend trait, and verifies repo-scoped runtime isolation with real filesystem tests. |
| 04 §映射与监听 | 重复信号触发 MUST 产生相同候选结果状态 | `crates/core/src/source_control/pending_fs.rs:78-134`<br>`crates/core/tests/watcher_idempotent_burst.rs:12-45` | ✅ implemented | `pending_fs::upsert` now skips byte-identical rewrites, preserving the original row bytes, and the burst watcher test asserts repeated modify signals keep the serialized pending row unchanged. |
| 04 §映射与监听 | 文件逻辑实体未确认删除前，`NodeId` MUST 保持稳定 | `crates/core/src/sync/watcher/dispatch.rs:49-109`<br>`crates/core/src/sync/pending_rename.rs:14-31`<br>`crates/core/tests/watcher_rename_pairing.rs:12-31` | ✅ implemented | Rename pairs from the real watcher resolve the tracked doc by inode/canonical mapping, write pending rename candidates with the original `doc_id`, and the integration test verifies the rename keeps `NodeId::from_doc_id(doc_id)` stable. |
| 04 §映射与监听 | Watcher side table 数据 MUST 严格绑定当前 Repo，严禁跨 Repo 污染 | `crates/core/src/sync/handler.rs:17-39`<br>`crates/core/src/sync/pending.rs:15-17`<br>`crates/core/src/ledger/manager/workspace.rs:53-96` | ✅ implemented | FS handler resolves `(repo_name, repo_id, repo_path)` first; pending helpers always call `run_on_local_repo(repo_name, ...)`; workspace resolution returns repo scope explicitly. |
| 04 §完整性与灾备 | 所有权威写操作 MUST 以日志追加形式记录，MUST NOT 原地修改 | `crates/core/src/ledger/ops_write_direct.rs:7-35`<br>`crates/core/src/ledger/ops_write_generated.rs:37-88` | ✅ implemented | Authoritative local append paths only insert into `LEDGER_OPS` with monotonic global seq; no in-place update path for local authoritative entries was found in scope. |
| 04 §完整性与灾备 | 系统 MUST 提供 Ledger 导出为 JSON Lines 的能力 | `apps/cli/src/commands/export.rs:1-116`<br>`apps/cli/src/export_entries.rs:1-118` | ✅ implemented | `deve export --format json` emits one JSONL row per `LEDGER_OPS` entry via `export_entries::build`, with live-proxy fallback when the server holds the DB lock. Roundtrip/monotonic test in `export_entries.rs::jsonl_roundtrip_is_monotonic_and_line_stable`. Earlier scoped scan missed `apps/cli/` — corrected 2026-04-09. |
| 04 §完整性与灾备 | Breaking Change 场景 MUST NOT 尝试复杂原地 Migration 脚本 | `crates/core/src/ledger/node_meta/migrate.rs:1-61` | ✅ implemented | Scope has a lightweight `migrate.rs` that only ensures tables/read paths; no complex in-place ledger migration script or exporter/importer was found under scope. |
| 04 §核心交互约束 | 后端对 File / Folder / Repo 的检索与操作 MUST 仅通过 `NodeId / RepoUUID` 完成，严禁 Path 作为主键 | `crates/core/src/ledger/manager/repo_db.rs:7-28`<br>`crates/core/src/ledger/manager/locator.rs:179-205`<br>`crates/core/src/ledger/doc_lookup.rs:6-16` | ✅ implemented | Repo execution resolves UUID first; doc lookup resolves `path -> node_id -> node_meta.doc_id`, with invariant that metadata-only residue must not re-enter main path. |
| 04 §核心交互约束 | 收到 Name/Path 后，后端 MUST 先查映射表获取唯一标识 | `crates/core/src/ledger/manager/metadata_ops.rs:44-56`<br>`crates/core/src/ledger/doc_lookup.rs:6-16` | ✅ implemented | Tracked doc lookup uses `path -> node_id -> node_meta.doc_id`; repo selector resolution maps name/id to single repo name first. |
| 04 §核心交互约束 | 所有业务逻辑执行 MUST 仅针对 UUID 进行 | `crates/core/src/ledger/manager/source_control_ops.rs:118-130`<br>`crates/core/src/ledger/manager/commit_apply.rs:14-30` | ⚠️ partial | Code resolves `doc_id` and then appends ops against that identity, but many public APIs still execute from path-shaped entrypoints (`stage_pending(path)`, `diff_doc_path(path)`, `commit_file_ops_in_local_repo(... normalized_path ...)`). |
| 04 §跨平台路径策略 | Ledger / DB / Cache 中的路径字符串 MUST 统一使用 `/` | `crates/core/src/ledger/metadata.rs:14-18`<br>`crates/core/src/ledger/node_meta/core.rs:11-18`<br>`crates/core/src/ledger/manager/workspace.rs:43-50` | ✅ implemented | Path storage/read paths normalize with `to_forward_slash`; workspace relative path also normalizes before formatting. |
| 04 §跨平台路径策略 | 从 OS 读入路径时 MUST 立即调用 `to_forward_slash` | `crates/core/src/sync/scan.rs:96-105`<br>`crates/core/src/ledger/manager/workspace.rs:60-63` | ✅ implemented | Scan converts repo-relative paths with `path_to_forward_slash`; workspace path resolution normalizes root-relative input immediately. |
| 04 §跨平台路径策略 | 所有路径操作 MUST 通过 `utils/path`，严禁手动 `replace` | `crates/core/src/plugin/manifest.rs:50-56` | ⚠️ partial | Plan: `所有路径操作 MUST 通过此模块进行，严禁手动通过字符串替换 (replace) 处理`.<br>Code: `let raw = path.to_string_lossy().replace('\\\\', \"/\");` in `manifest.rs:56`. |
| 04 §浏览器存储分层 | 首次进入 repo 时生成的 Ed25519 私钥 MUST 为 `extractable: false` | `None in scoped dirs` | ❌ missing | `rg "WebCrypto|IndexedDB|extractable|generateKey"` under scope returned no hit. |
| 04 §恢复语义 | 仅 Cookie 可用、IndexedDB 不可用时，UI MUST 切换只读并禁止 `SyncPush` | `None in scoped dirs` | ❌ missing | `rg "IndexedDB|DegradedSyncMode|SyncPush"` under scope returned no implementation hit. |
| 05 §移动端 P2P | 前台移动端 MUST 作为完整 P2P Peer 参与 Gossip / 握手 / 增量同步 | `None in scoped dirs` | ❌ missing | No mobile/foreground-specific sync logic was found under scope (`rg "mobile|background|light-peer"` only hit tests/comments). |
| 05 §移动端 P2P | 后台移动端 MUST 立即降级为 Light Peer | `None in scoped dirs` | ❌ missing | No background/light-peer mode switching implementation was found in scoped modules. |
| 05 §移动端 P2P | 移动端恢复前台时 MUST 触发 `SyncHello` 并补齐缺失 | `None in scoped dirs` | ❌ missing | No mobile resume hook or `SyncHello` message type implementation was found under scope. |
| 05 §移动端 P2P | Light Peer 状态下 MUST 禁止长时合并与大批量重放 | `None in scoped dirs` | ❌ missing | No light-peer write boundary logic was found in scoped sync/security/plugin/ledger code. |
| 05 §移动端 P2P | 后台窗口期更新 MUST 由 Server 托管，恢复前台后增量拉取 | `None in scoped dirs` | ❌ missing | No mobile background durability policy or relay-specific storage contract was found in scope. |
| 05 §主节点/代理节点 | 浏览器 MUST 优先连当前 origin 的 `relative /ws` | `None in scoped dirs` | ❌ missing | No `/ws` route selection or browser endpoint policy exists in the scoped core modules. |
| 05 §主节点/代理节点 | 生产环境 MUST 通过单一配置端点或反向代理提供稳定入口 | `None in scoped dirs` | ❌ missing | No production endpoint routing policy exists in the scoped core modules. |
| 05 §协议类型 | 进入同步路径的消息 MUST 携带可确定路由的 `repo_id` | `crates/core/src/sync/protocol.rs:20-47` | ✅ implemented | `SyncRequest`, `SyncSnapshotRequest`, and `SyncResponse` all carry `repo_id`. |
| 05 §握手 | 后续 `SyncRequest` / `SyncPush` / `SyncSnapshotRequest` / `SyncPushSnapshot` / 广播 MUST 沿用同一 `repo_id`，否则拒绝/断连 | `crates/core/src/sync/protocol.rs:20-47`<br>`crates/core/src/sync/engine/handshake.rs:14-24` | ⚠️ partial | Protocol types are repo-scoped and diff computation requires explicit `repo_id`, but no reject/disconnect enforcement for mismatched follow-up messages was found in scope. |
| 05 §握手 | `SyncHello` MUST 提供 `repo_id` 与当前 vector | `crates/core/src/sync/engine/handshake.rs:14-24`<br>`crates/core/src/sync/engine/handshake.rs:32-79` | ⚠️ partial | Handshake API takes `repo_id` and `remote_vector`, but no `SyncHello` message struct exists in scope. |
| 05 §握手 | `SyncRequest` MUST 至少携带 `{ repo_id, known_vector }`，不得依赖隐式默认 repo | `crates/core/src/sync/protocol.rs:20-29` | ⚠️ partial | `SyncRequest` carries `repo_id`, but the second field is `range`, not `known_vector`. |
| 05 §握手 | `Snapshot` MUST 携带所属 `repo_id` 与生成时的 server vector | `crates/core/src/sync/protocol.rs:38-47`<br>`crates/core/src/sync/engine/transfer/snapshot.rs:9-13` | ⚠️ partial | `SyncResponse` carries `repo_id`; no server vector field exists in the response type. |
| 05 §同步流程 | 所有 Repo 数据在传输前 MUST 使用 `RepoKey` 加密（AES-256-GCM） | `crates/core/src/security/cipher.rs:5-27`<br>`crates/core/src/sync/engine/transfer/mod.rs:9-39`<br>`crates/core/src/sync/engine/transfer/snapshot.rs:13-40` | ✅ implemented | `RepoKey` wraps `Aes256Gcm`; both incremental and snapshot transfer encrypt each `LedgerEntry` before sending. |
| 05 §同步流程 | B 写入成功后 MUST 更新本地记录的对端 VC 到最新 Seq | `crates/core/src/sync/engine/transfer/apply.rs:13-28`<br>`crates/core/src/sync/engine/transfer/apply.rs:38-49` | ✅ implemented | Snapshot path does `set_exact(response.peer_id, max_seq)`; incremental path does `version_vector.update(response.peer_id, max_seq)`. |
| 05 §同步流程 | B 作为镜像端 MUST 直接接受来自 A 的已校验数据 | `crates/core/src/sync/engine/transfer/apply.rs:17-23`<br>`crates/core/src/ledger/manager/repo_db.rs:50-63` | ✅ implemented | Remote apply decrypts then directly calls `append_remote_op` into shadow repo keyed by `peer_id + repo_id`; no merge/conflict step exists in this path. |
| 05 §重连策略 | 重连成功后 MUST 重新发送当前 repo 的 `SyncHello`；切换 repo 后必须以新 `repo_id` 重建上下文 | `None in scoped dirs` | ❌ missing | No reconnection state machine or `SyncHello` resend logic was found in scoped modules. |
| 05 §重连策略 | 服务端返回 `401/403` 或 `AUTH_*` 后，客户端 MUST 停止普通重连循环并转入 Unauthorized | `crates/core/src/security/auth/jwt.rs:57-77` | ❌ missing | Auth primitives exist, but no reconnect client loop or Unauthorized state transition exists in scope. |
| 05 §间接同步 | B 对陌生 A 的 offer MUST Ignore | `crates/core/src/sync/engine/handshake.rs:27-79` | ❌ missing | Handshake verifies peer id and signature, but no trusted-list / stranger-filter logic was found in scope. |
| 05 §间接同步 | Relay C MUST NOT 把 A 的数据传给陌生 B | `None in scoped dirs` | ❌ missing | No relay-side payload blocking or trust matrix enforcement exists in scoped modules. |
| 05 §间接同步 | 经 C 中转但签名来源是 A 时，B MUST 写入 `ledger/remotes/peer_a/`，绝不可写入 `peer_c` | `crates/core/src/sync/engine/transfer/apply.rs:17-23`<br>`crates/core/src/ledger/shadow/management.rs:31-40` | ⚠️ partial | Storage path is keyed by `response.peer_id` and ends up under `remotes/<peer_id>/...`; no per-payload signature/source binding was found at apply time, so the “只要签名验证通过显示来源为 A” part is not enforced here. |
| 05 §相关配置 | 移动端后台时 `SYNC_MODE` MUST 强制覆盖为 `light-peer` | `None in scoped dirs` | ❌ missing | No mobile/background override for `SYNC_MODE` was found in scope. |
| 10 §AI Agent 顶层约束 | 若安全边界不明确，外部 CLI Agent MUST NOT 默认启用 | `None in scoped dirs` | ✅ implemented | No CLI agent bridge/process launcher was found under scope (`rg "AGENT_CLI_PATH|Command::new|std::process::Command"` returned no hit). |
| 10 §Native AI Chat | Native AI Chat MUST 支持 chat、多轮、读当前 Markdown/只读上下文、`/plan`、`/build`、`/agents` | `crates/core/src/plugin/runtime/host/chat.rs:92-130`<br>`crates/core/src/plugin/runtime/host/note.rs:14-53` | ⚠️ partial | Chat streaming exists (`ai_chat_stream`, `ai_chat_stream_with_tools`) and managed markdown can be read with `note_read`; no `/plan`, `/build`, or `/agents` mode implementation was found in scope. |
| 10 §Native AI Chat | Native AI Chat MUST NOT 默认支持 MCP / Skills / 直接 Source Control 写 / 无确认文件修改 | `apps/cli/src/server/ai_chat/mod.rs`<br>`apps/cli/src/server/ai_chat/stream.rs`<br>`crates/core/src/plugin/runtime/tools.rs`<br>`crates/core/src/plugin/runtime/host/skill.rs` | ✅ updated | MCP runtime/host entry has been removed. Native AI keeps explicit fail-closed handling for request tools and provider tool calls; Skills remain a separate opt-in host capability, not a default Native AI Chat tool loop. |
| 10 §原生模式 | PLAN 模式 MUST NOT 调用任何工具 | `crates/core/src/plugin/runtime/host/chat.rs:110-129`<br>`crates/core/src/plugin/runtime/tools.rs:68-189` | ❌ missing | Tool-capable chat entrypoint exists (`ai_chat_stream_with_tools`), but no PLAN-mode gate was found in scope. |
| 10 §原生模式 | BUILD 模式执行路径 MUST 是受限宿主能力，不得等价于 shell / MCP / Skills | `crates/core/src/plugin/runtime/host/note.rs:56-68`<br>`crates/core/src/plugin/runtime/host/skill.rs:50-96` | ⚠️ partial | Managed note writes go through restricted host functions and MCP host APIs no longer exist. Skill APIs still require explicit capability and are not part of default Native AI Chat. |
| 10 §原生模式 | `/agents` MUST NOT 用作后端切换命令 | `None in scoped dirs` | ❌ missing | No `/agents` command implementation or mode router was found in scope. |
| 10 §原生模式 | `/agents` MUST NOT 隐式拉起 Trusted External Agent | `None in scoped dirs` | ✅ implemented | No trusted external agent launcher exists under scope. |
| 17 §Trusted External Agent Runtime | Trusted External Agent MUST NOT 被视为通用插件市场能力 | `crates/core/src/plugin/mod.rs`<br>`crates/core/src/plugin/manifest.rs:17-47` | ✅ implemented | Scoped plugin system is Rhai-manifest based; no trusted external agent marketplace/runtime integration exists in scope. |
| 17 §通用边界 | 若未来需要写托管笔记，MUST 走 ledger-aware host functions，而不是裸 `fs_write` | `crates/core/src/plugin/runtime/host/fs.rs:35-56`<br>`crates/core/src/plugin/runtime/host/note.rs:56-74`<br>`crates/core/src/plugin/runtime/host/path_guard.rs:87-101` | ✅ implemented | `fs_write` rejects ledger-managed targets; `note_write` resolves managed markdown to repo/doc identity, appends ledger facts, then persists projection. |
