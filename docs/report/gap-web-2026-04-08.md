| Plan §Section | Clause (one line) | Code Location | Status | Evidence |
|---|---|---|---|---|
| 03 §Interaction Philosophy | Cursor Reveal: when cursor touches/enters rendered source range, render layer MUST immediately yield to raw Markdown | — | ❌ missing | No Markdown widget/render-layer component in `apps/web/src/editor/**`; no cursor-range reveal hook found in `apps/web/src/**`. |
| 03 §Link Navigation | Rendered external links MUST include `target="_blank"` and `rel="noopener noreferrer"` | `apps/web/src/utils/markdown.rs:119-129` | ✅ implemented | `render_link_open()` appends `target="_blank" rel="noopener noreferrer"`. |
| 03 §Phase 1 | Fenced code MUST be atomic and only syntax-highlighted | `apps/web/src/utils/markdown.rs:30-50`, `apps/web/src/utils/markdown.rs:80-95` | ⚠️ partial | Plan: `解析器 MUST 将其视为原子块...仅执行语法高亮。` Code: `Event::Start(Tag::CodeBlock(kind))` consumes block atomically, but `render_code_block()` emits `<pre><code class="language-...">` with no highlighter call. |
| 03 §Phase 1 | Block math (`$$`) MUST be atomic and sent to LaTeX engine | `apps/web/src/components/outline_render/mod.rs:20-22`, `apps/web/src/components/outline_render/parse.rs:55-61` | ⚠️ partial | Plan: `Block Math ($$)... MUST 将其视为原子块，内容直接传递给 LaTeX 引擎。` Code only renders outline-inline math via `render_katex_to_string(&seg.text)`; no editor/chat block-math renderer found. |
| 03 §Phase 2 | Higher-priority inline elements MUST NOT render lower-priority elements inside | — | ❌ missing | No general inline Markdown parser/hook in `apps/web/src/editor/**`; only outline/chat-specific helpers exist. |
| 03 §Phase 2 | Inline code MUST consume backticks first and protect inner syntax | `apps/web/src/components/outline_render/parse.rs` | ⚠️ partial | Outline parser has inline segmentation, but no editor-wide/parser-wide enforcement for rendered Markdown in `apps/web/src/**`. |
| 03 §Phase 2 | Escaping MUST handle `\\$`, `\\|`, `\\` correctly | `apps/web/src/components/outline_render/parse.rs` | ⚠️ partial | Outline parser handles limited inline parsing; no general Markdown escape handler for editor/chat rendering beyond `pulldown_cmark` default. |
| 03 §Code Blocks | Code block toolbar MUST show two buttons: Copy and Ellipsis | `apps/web/src/utils/markdown.rs:89-95` | ⚠️ partial | Plan: `MUST 显示两个按钮（从左到右）：Copy ... Ellipsis (...)` Code renders one button only: `<button class="apply-code"...>{}</button>`. |
| 03 §Nested Hell | Rendering engine MUST support arbitrary recursive nesting | `apps/web/src/components/outline_render/mod.rs`, `apps/web/src/utils/markdown.rs` | ⚠️ partial | Nested rendering exists for outline/chat HTML, but no evidence of full editor recursive mixed-widget renderer in `apps/web/src/**`. |
| 03 §Nested Hell | Each nesting level MUST have clear visual indentation | `apps/web/src/components/outline.rs:86-91`, `apps/web/src/components/sidebar/item.rs:31-32` | ⚠️ partial | Indentation exists via `padding-left`, but no `--depth`/gradient-based nesting system from plan is present. |
| 03 §Nested Hell | Code blocks inside blockquotes MUST use layered `background-image` composition | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`; implementation was specified in CSS outside this scope. |
| 08 §Responsive Mapping | Web UI MUST map mobile viewport to Mobile spec and large viewport to Desktop spec | `apps/web/src/components/main_layout_contexts.rs:22-33`, `apps/web/src/components/main_layout_body.rs:25-63` | ✅ implemented | `use_mobile_breakpoint()` sets `width <= 768.0`; `MainLayoutBody` switches between `<MobileLayout>` and `<DesktopLayout>`. |
| 08 §Command First | All capabilities MUST have unique `CommandId` and be callable via `Cmd+K` | `apps/web/src/components/command_palette/types.rs:10-18`, `apps/web/src/components/command_palette/registry.rs:20-110` | ⚠️ partial | Plan: `所有功能 ... MUST 拥有唯一的 CommandId，且可通过命令面板 (Cmd+K) 调用。` Code defines some `Command { id, title, action }`, but registry only covers a small subset (`open`, `settings`, `lang`, `switch_peer`, `establish_branch`, `merge_peer`, `toggle_ai_chat`). |
| 08 §Offline Priority | Mobile/Desktop core features MUST work offline | — | ❌ missing | No corresponding web component/hook for native offline guarantee in `apps/web/src/**`. |
| 08 §Design Tokens | System MUST use CSS variables; no hardcoded hex values | `apps/web/src/components/diff_view/header.rs:27-64`, `apps/web/src/editor/mod.rs:75-95`, `apps/web/src/components/disconnect_overlay.rs:17-27` | ⚠️ partial | Plan: `MUST 使用 CSS 变量...严禁硬编码 Hex 值。` Code mixes CSS vars with utility color classes like `bg-white`, `border-gray-200`, `bg-amber-100`, `text-red-400`. |
| 08 §Z-Index Registry | System MUST strictly follow z-index layer registry | `apps/web/src/components/disconnect_overlay.rs:17`, `apps/web/src/components/search_box/ui_sheet_style.rs:38-40`, `apps/web/src/components/main_layout_runtime.rs:39-46` | ⚠️ partial | Code uses ad hoc values `z-[9999]`, `z-[100]`, `z-50`, not the documented `L0..L6` registry. |
| 08 §Iconography | System MUST use `lucide-leptos` crate | `apps/web/src/components/icons/mod.rs:1-5`, `apps/web/src/components/icons/mod.rs:10-25` | ⚠️ partial | Plan: `MUST 使用 lucide-leptos crate。` Code: `轻量级图标组件（基于 Lucide Design），零外部依赖。` |
| 08 §Focus Management | Modal open MUST trap focus and keep Tab cycling inside modal | `apps/web/src/components/command_palette/ui.rs:21-107` | ❌ missing | Command palette overlay has `autofocus`, backdrop close, and keydown handling, but no focus-trap/tab-cycle logic. |
| 08 §Focus Management | Modal close MUST restore focus to previous state | `apps/web/src/components/command_palette/ui.rs:21-107` | ❌ missing | No saved previous focus handle or restore callback in command palette/search modal code. |
| 08_web §Scope Boundary | Mobile/Desktop MUST use Tauri v2 | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_web §Single Binary | CLI binary MUST embed frontend static assets | — | ❌ missing | Not implemented in `apps/web/src/**`; no Leptos component/hook corresponds to build embedding. |
| 08_web §SPA Routing | Backend MUST implement SPA route fallback | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_web §Viewport Mapping | Web MUST map by viewport width | `apps/web/src/components/main_layout_contexts.rs:22-33` | ✅ implemented | `set_is_mobile.set(width <= 768.0);`. |
| 08_web §Viewport Mapping | `W_view <= 768px`: Web UI MUST match Mobile spec | `apps/web/src/components/main_layout_body.rs:26-39`, `apps/web/src/components/mobile_layout/mod.rs:35-121` | ✅ implemented | Mobile layout branch is selected for `is_mobile.get()`. |
| 08_web §Viewport Mapping | `W_view > 768px`: Web UI MUST match Desktop spec | `apps/web/src/components/main_layout_body.rs:40-63`, `apps/web/src/components/desktop_layout.rs:24-99` | ✅ implemented | Desktop layout branch is selected when `!is_mobile.get()`. |
| 08_web §Safety Constraints | On disconnect, UI MUST be locked by overlay, block writes, and show reconnecting state | `apps/web/src/components/main_layout_runtime.rs:73`, `apps/web/src/components/disconnect_overlay.rs:11-30`, `apps/web/src/hooks/use_core/write_gate_logic.rs:40-56` | ✅ implemented | Overlay mounted globally; non-connected statuses show fullscreen lock UI; write gate returns `Offline`/`Reconnecting`. |
| 08_web §Safety Constraints | On session expiry/auth reject, UI MUST NOT keep infinite reconnect overlay; must switch to explicit unauthenticated state | `apps/web/src/components/disconnect_overlay.rs:13-15`, `apps/web/src/components/main_layout_setup.rs:24-32`, `apps/web/src/app.rs:102-115` | ✅ implemented | Overlay is suppressed for `ConnectionStatus::Unauthorized`; `watch_session_expired()` and auth probe route to `LoginPage`. |
| 08_web §Safety Constraints | Dashboard data MUST NOT persist to IndexedDB | `apps/web/src/components/dashboard/mod.rs:1-8` | ✅ implemented | File header states: `所有指标仅存于 RAM 信号中，不持久化到 IndexedDB。` No dashboard storage hook is referenced. |
| 08_web §Dashboard Auth | Dashboard MUST require authenticated identity; unauthenticated access redirects to Login | `apps/web/src/app.rs:102-115`, `apps/web/src/components/desktop_layout_content.rs:26-39`, `apps/web/src/components/mobile_layout/content.rs:37-49` | ✅ implemented | `App` gates all main layout/dashboard rendering behind `AuthState::Authenticated`; else shows `LoginPage`/`AuthUnavailablePage`. |
| 08_web §External Edit Flow | External FS changes MUST NOT go directly into Ledger; use repo-scoped `pending_fs_ops` | — | ❌ missing | No `pending_fs_ops`/`FsChangeDetected` UI hook in `apps/web/src/**`. |
| 08_web §External Edit Flow | Disk-change feedback MUST NOT overwrite editor confirmed+pending overlay | — | ❌ missing | No disk-change notice or external-edit reconciliation component/hook found in `apps/web/src/**`. |
| 08_web §Resizable Layout | Sidebar/right-panel widths MUST persist via `localStorage` | `apps/web/src/hooks/use_layout.rs:30-62`, `apps/web/src/hooks/use_layout_storage.rs:5-15` | ✅ implemented | Reads `ui_sidebar_width` / `ui_right_panel_width`; effects write widths back to `localStorage`. |
| 08_web §Resizable Layout | Outer gutter MUST be draggable and persisted | `apps/web/src/hooks/use_layout.rs:35-45`, `apps/web/src/hooks/use_layout.rs:82-113` | ✅ implemented | `outer_gutter` has resize callbacks and persists via `ui_outer_gutter`. |
| 08_web §Status Copy | UI MUST distinguish `session token` state and `peer identity` state | `apps/web/src/hooks/use_core/status_summary.rs:54-73`, `apps/web/src/api/service.rs:77-105` | ⚠️ partial | Session/auth and repo writer readiness are separate (`ConnectionStatus::Unauthorized` vs `writer_ready_for()`), but UI text summarizes them into single states like `Session Expired`, `HandshakingRepo`, `ReadOnly`, not explicit combined copy such as “logged in / peer unregistered”. |
| 08_web §Status Copy | UI MUST distinguish `network disconnected` and `session expired` | `apps/web/src/hooks/use_core/status_summary.rs:54-58`, `apps/web/src/components/disconnect_overlay.rs:13-15`, `apps/web/src/app.rs:94-106` | ✅ implemented | Separate states: `Unauthorized -> SessionExpired`; `Disconnected -> Offline`; overlay hides on unauthorized and auth screen takes over. |
| 08_web §Status Copy | Repo switch MUST show `Handshaking repo...` until repo-scoped peer registration completes | `apps/web/src/hooks/use_core/status_summary.rs:62-70`, `apps/web/src/i18n/bottom_bar.rs:48-53`, `apps/web/src/components/bottom_bar/status.rs:56-58` | ✅ implemented | `HandshakingRepo` state renders `t::bottom_bar::handshaking_repo()`. |
| 08_web §DegradedSyncMode | If IndexedDB/WebCrypto unavailable, top banner MUST show read-only reason | `apps/web/src/storage/mod.rs:21-48`, `apps/web/src/hooks/use_core/storage_runtime_bootstrap.rs:16-33`, `apps/web/src/hooks/use_core/storage_runtime_effect.rs:12-19`, `apps/web/src/components/desktop_layout_banner.rs:4-10`, `apps/web/src/components/mobile_layout/layout_banner.rs:5-10` | ✅ implemented | Capability probe derives `DegradedSyncMode`; `banner_text()` names `WebCrypto/IndexedDB/Ed25519`; banner is shown in desktop/mobile shells. |
| 08_web §Editing Guardrails | Read-only mode MUST disable/hide editor, create button, and sync push button | `apps/web/src/editor/mod.rs:55-66`, `apps/web/src/components/sidebar/explorer_header.rs:29-52`, `apps/web/src/components/sidebar/source_control/commit_actions.rs:21-55` | ✅ implemented | `ffi::set_read_only(should_readonly)` gates editor; create button hidden behind `can_write`; commit/push buttons are disabled from `write_block`/`can_commit_now`. |
| 08_web §Editing Guardrails | If session valid but peer identity missing, UI MUST allow retry registration instead of silent failure | `apps/web/src/hooks/use_core/effects/handshake.rs:68-88`, `apps/web/src/hooks/use_core/effects/handshake_cycle_connected_attempt.rs:29-61` | ⚠️ partial | Handshake retries exist internally, but no explicit user-facing “retry registration” control was found in `apps/web/src/components/**`. |
| 08_web §Repo Switch Flow | Refresh restore MUST bind by `repo_id`; `repo_name` display only | `apps/web/src/hooks/use_core/scope_prefs.rs:11-17`, `apps/web/src/hooks/use_core/scope_prefs.rs:78-98` | ✅ implemented | Persisted scope stores both, but only valid UUID `repo_id` is accepted; parse fails unless `uuid::Uuid::parse_str(&scope.repo_id).is_ok()`. |
| 08_web §Repo Switch Flow | `switch_nonce` MUST be strictly greater than current `scope_nonce` | `apps/web/src/hooks/use_core/switch_nonce.rs:5-13`, `apps/web/src/hooks/use_core/callbacks_switch_repo.rs:26-37`, `apps/web/src/hooks/use_core/callbacks_switch_branch.rs:33-48` | ✅ implemented | `next_switch_nonce_after(current_scope_nonce)` guarantees `> current_scope_nonce` before sending switch messages. |
| 08_web §Activity Bar More | Full-row menu click MUST switch view | `apps/web/src/components/activity_bar/popup_menu.rs:25-33`, `apps/web/src/components/mobile_layout/drawers/left_more_menu.rs:55-64` | ✅ implemented | Row button uses `select_view.run(item)`. |
| 08_web §Activity Bar More | `Pin/Unpin` MUST be a separate action, not same click semantics | `apps/web/src/components/activity_bar/popup_menu.rs:34-54`, `apps/web/src/components/mobile_layout/drawers/left_more_menu.rs:65-79` | ✅ implemented | Separate pin button calls `toggle_pin.run(item)` / `toggle_pin(item)`; row click selects view. |
| 08_web §Open In New Window | New-window link MUST preserve existing query params and append `doc=...` correctly | `apps/web/src/components/sidebar/item_action.rs:50-57`, `apps/web/src/components/sidebar/item_action.rs:104-115` | ✅ implemented | `build_new_window_url()` keeps existing `?`, appends `&`/`?` correctly, and preserves hash suffix. |
| 08_web §Implementation Boundary | Web offline capability MUST NOT replace embedded service | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Web Mapping | For `W_view <= 768px`, Web UI MUST follow Mobile spec | `apps/web/src/components/main_layout_contexts.rs:22-33`, `apps/web/src/components/mobile_layout/mod.rs:35-121` | ✅ implemented | Same breakpoint and dedicated mobile shell. |
| 08_mobile §HTML Header | Mobile HTML header MUST include viewport settings for notch/zoom prevention | — | ❌ missing | No matching file in `apps/web/src/**`; `<head>`/meta viewport is outside this scope. |
| 08_mobile §Safe Area | All fixed-position elements MUST use CSS `env()` safe-area insets | `apps/web/src/components/mobile_layout/header.rs:19-22`, `apps/web/src/components/mobile_layout/footer.rs:74-77`, `apps/web/src/components/mobile_layout/toolbar.rs:43-51`, `apps/web/src/components/mobile_layout/drawers/left_header.rs:12-15`, `apps/web/src/components/mobile_layout/drawers/right.rs:20-23` | ✅ implemented | Fixed mobile header/footer/toolbar/drawer headers use `env(safe-area-inset-top/bottom)`. |
| 08_mobile §Accessory View | System MUST render Accessory View above soft keyboard | `apps/web/src/components/mobile_layout/toolbar.rs:5-65`, `apps/web/src/components/mobile_layout/layout_frame.rs:95-105` | ✅ implemented | `MobileAccessoryToolbar` is shown when `keyboard_offset > 0` and current doc is active. |
| 08_mobile §Interactive Safety | Edge swipe MUST NOT steal clicks from edge interactive controls | `apps/web/src/components/mobile_layout/gesture.rs:81-123` | ✅ implemented | `resolve_swipe_target(..., interactive_target)` returns `None`; `is_interactive_target()` excludes buttons/links/inputs and `[data-no-edge-swipe]`. |
| 08_mobile §Diff View | Mobile MUST NOT use side-by-side diff; must force Unified View | `apps/web/src/components/mobile_layout/content.rs:55-69`, `apps/web/src/components/diff_view/mod.rs:41-56` | ✅ implemented | Mobile passes `force_unified=true` into `DiffView`. |
| 08_mobile §Spectator Mode | Mobile MUST insert orange 24px `Read-Only Mode` banner below top bar | `apps/web/src/components/mobile_layout/content.rs:31-35` | ✅ implemented | Mobile content renders `h-6` (`24px`) orange banner with `t::common::read_only_mode()`. |
| 08_mobile §Bottom Bar | Collapsed default state MUST show one line: `Branch / Ready / Words / Lines / Col` | `apps/web/src/components/mobile_layout/footer_summary.rs:30-69` | ⚠️ partial | Plan: `MUST 仅显示一行：Branch / Ready / Words / Lines / Col。` Code shows one row, but pills are `BranchSwitcher`, status, `Words`, `Lines`, and `Chars` (`t::bottom_bar::chars`), not `Col`. |
| 08_mobile §More Menu | `More(...)` MUST reuse desktop semantics: row click switches view; `Pin/Unpin` only changes pinned state | `apps/web/src/components/mobile_layout/drawers/left_more_menu.rs:55-79` | ✅ implemented | Row click runs `select_view`; pin button `stop_propagation()` and toggles pinned only. |
| 08_mobile §Memory | Low-end devices MUST run smoothly | — | ❌ missing | No measurable performance guard/hook in `apps/web/src/**` proving this clause. |
| 08_mobile §Dependency | Mobile MUST avoid heavy UI frameworks | `apps/web/src/components/icons/mod.rs:1-5`, `apps/web/src/components/mobile_layout/mod.rs:1-22` | ✅ implemented | Mobile UI is handwritten Leptos modules; icon layer explicitly states `零外部依赖`. |
| 08_mobile §Consistency | Interaction/layout rules MUST match this mobile chapter, not desktop defaults | `apps/web/src/components/mobile_layout/mod.rs:35-121` | ⚠️ partial | Dedicated mobile shell exists, but several details still differ from chapter text, e.g. footer collapsed summary uses `Chars` instead of `Col`. |
| 08_mobile §Embedded Service | Backend service MUST be embedded and auto-start on app launch | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Service Boot | App MUST start embedded service before UI | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Random Port | Port MUST use random available loopback port in memory | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Port Conflict | MUST auto-fallback to a new port on conflict | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §IPC Fallback | MUST provide IPC fallback if port access is restricted | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Local IPC Security | Local communication MUST reject unauthorized cross-process access | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §IPC Auth | IPC MUST have process-level auth and session binding | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Loopback Bind | Service MUST listen only on `127.0.0.1` | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Offline-First | Without network, app MUST provide full read/write capability | — | ❌ missing | Web thin-client write path still blocks on disconnect via `RepoWriteBlock::Offline`. |
| 08_mobile §Persistence | All content MUST persist to local DB and Vault | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Crash Safety | MUST recover to last persisted state after crash | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Migration Boundary | Mobile UI MUST NOT define storage-migration semantics itself | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Encryption | At-rest storage MUST support encryption bound to secure hardware | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Key Rotation | MUST support key rotation/revocation without data loss | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Recovery | MUST provide key recovery strategy | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Backup | MUST support local encrypted backup | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Least Privilege | Local operations MUST follow least-privilege principle | — | ❌ missing | No corresponding component/hook in `apps/web/src/**`. |
| 08_mobile §Size | App size MUST stay controlled; avoid heavy deps | `apps/web/src/components/icons/mod.rs:1-5` | ✅ implemented | Code explicitly uses local lightweight icon components with `零外部依赖`; no heavy UI framework module found in mobile shell. |
| 08_mobile §Bottom Sheet | MUST have close threshold to prevent accidental dismissal | `apps/web/src/components/search_box/sheet_gesture.rs:5-10`, `apps/web/src/components/search_box/sheet_gesture.rs:63-64` | ✅ implemented | `CLOSE_THRESHOLD_PX: 72`. |
| 08_mobile §Bottom Sheet | MUST debounce micro-movements to avoid accidental dismissal | `apps/web/src/components/search_box/sheet_gesture.rs:6-8`, `apps/web/src/components/search_box/sheet_gesture.rs:53-56` | ✅ implemented | `QUICK_TAP_MS: 90.0`, `QUICK_TAP_MAX_DRAG_PX: 20`, early-return on small quick drags. |
| 08_mobile §Bottom Sheet | MUST avoid scroll conflict; only dismiss from top/dismiss intent | `apps/web/src/components/search_box/sheet_gesture.rs:87-107` | ✅ implemented | `can_start_dismiss()` blocks starts from `[data-sheet-results='1']` and only allows drag-handle-origin dismiss. |
| 08_mobile §Drawer Reachability | MUST unify header-bar and close-button interaction semantics | `apps/web/src/components/mobile_layout/drawers/left_header.rs:12-24`, `apps/web/src/components/mobile_layout/drawers/right.rs:20-34` | ✅ implemented | Both drawers use `h-12` headers and `h-11 min-w-11` close controls with same transition classes. |
| 08_mobile §Touch Feedback | Sidebar / Outline / Search Result `selected` / `hover` / `active` semantics MUST stay consistent | `apps/web/src/components/sidebar/item.rs:67-75`, `apps/web/src/components/outline.rs:89-96`, `apps/web/src/components/search_box/result_item.rs:53-64` | ⚠️ partial | Plan: `selected、hover、active 语义 MUST 保持一致。` Code differs: sidebar uses `hover:bg-hover active:bg-active bg-accent-subtle`; outline uses `hover:bg-hover active:bg-hover`; search result uses `bg-accent-subtle`/`hover:bg-hover` and no shared active class. |
| 11_i18n §UI Text | Core UI text MUST all go through `t::xxx::yyy()` facade; no hardcoded strings | `apps/web/src/components/command_palette/ui.rs:99-105`, `apps/web/src/components/activity_bar/popup_menu.rs:38-49`, `apps/web/src/components/dashboard/mod.rs:49`, `apps/web/src/editor/mod.rs:83-96` | ⚠️ partial | Plan: `核心 UI 文本 MUST 全部通过 t::xxx::yyy() facade...` Code still contains hardcoded UI strings such as `Up/Down to navigate`, `Pin`/`Unpin`, `{} docs in current repo`, `Spectator Mode (Read Only)`, `Toggle Outline`. |
| 11_i18n §Format Handling | Date/Time/Number MUST use `Intl` API or Rust `time`; no manual assembly | `apps/web/src/utils/time.rs:21-35`, `apps/web/src/components/chat/message_item.rs:39-42` | ⚠️ partial | Plan: `严禁手动拼装... MUST 使用 Intl API...` Code uses manual formatting: `format!(\"{}-{:02}-{:02}\", ...)` and `format!(\"{:02}:{:02}\", ...)`. |
| 11_i18n §Backend Errors | Backend API MUST NOT return natural-language errors; MUST return standard codes | `apps/web/src/i18n/server_error.rs:4-56`, `apps/web/src/i18n/login.rs:83-97`, `apps/web/src/hooks/use_core/effects/message_protocol.rs:82-96` | ⚠️ partial | Frontend is code-based (`server_error::message(locale, error.code)` / `auth_error(locale, code)`), but scope does not contain backend implementation; no `apps/web/src/**` code can prove the backend-side MUSTs. |
| 11_i18n §Fallback | Missing locale key MUST fall back to `en-US` | `apps/web/src/i18n/mod.rs:37-42`, `apps/web/src/app.rs:35` | ⚠️ partial | `Locale::default()` is `En`, but there is no runtime missing-key fallback mechanism; translation functions are compile-time matches only. |
| 11_i18n §Detection | Locale detection MUST prefer user config, then `navigator.language`, then `en-US` | `apps/web/src/app.rs:35` | ❌ missing | App initializes `let locale = RwSignal::new(Locale::default());`; no user-config read or `navigator.language` probe in `apps/web/src/**`. |
| 11_i18n §Error Display Rule | Frontend display/branching MUST rely only on `code`, not natural-language `detail` | `apps/web/src/components/sidebar/source_control/error_notice_copy.rs:51-70`, `apps/web/src/components/login/page.rs:118-129`, `apps/web/src/hooks/use_core/effects/message_protocol.rs:84-96` | ⚠️ partial | Plan: `前端展示与分支判断 MUST 仅依赖 code，不得依赖自然语言 detail。` Code displays raw detail in `_ if notice.detail.is_some() => notice.detail.clone()` and builds transport messages with raw `message`. |
| 16 §Invariants | Web MUST NOT hold private copy of confirmed business state | `apps/web/src/editor/message_effect.rs:22-31`, `apps/web/src/editor/sync/history.rs:17-28` | ⚠️ partial | Plan forbids private confirmed state, but web keeps local `history` and `content` signals (`set_history`, `set_content`) while also reconciling against server history. |
| 16 §Invariants | `Auth for Write` and `Handshake for Sync` MUST be modeled separately | `apps/web/src/app.rs:39-40`, `apps/web/src/api/service.rs:72-105`, `apps/web/src/hooks/use_core/write_gate_logic.rs:40-55` | ✅ implemented | Auth uses `AuthState` / `ConnectionStatus::Unauthorized`; sync/write readiness uses separate `handshake_ready` and repo-scoped `writer_ready_repo_id`. |
| 16 §Invariants | Write-ready state MUST be repo-scoped, not connection-scoped | `apps/web/src/api/service.rs:18-21`, `apps/web/src/api/service.rs:77-105`, `apps/web/src/hooks/use_core/effects/message_dispatch_write.rs:16-22` | ✅ implemented | `mark_writer_ready(repo_id, peer_id)` stores `writer_ready_repo_id`; `writer_ready_for(repo_id)` checks exact repo match. |
| 16 §Invariants | File switch/reconnect/snapshot refresh MUST rebuild from confirmed + pending overlay | `apps/web/src/editor/sync/history.rs:14-31`, `apps/web/src/editor/sync/history_replay.rs:22-30` | ✅ implemented | History reconciliation runs `pending::reconcile_with_history(...)` then `replay_pending_overlay(ctx)`. |
| 16 §Invariants | Commit/Delete/Merge final validity MUST come from ledger append/anchor, not metadata/snapshot side effects | `apps/web/src/hooks/use_core/callbacks_sync_write.rs:23-85`, `apps/web/src/components/sidebar/source_control/commit_actions.rs:32-79` | ⚠️ partial | Web UI only sends protocol commands and does not locally finalize outcomes; no `apps/web/src/**` evidence of ledger-anchor confirmation path beyond waiting for server responses. |
| 16 §Edit Intent | `ClientMessage::Edit` MUST carry `doc_id`, `op`, `client_id`, `client_op_id` | `apps/web/src/editor/delta_input_forward.rs:27-46`, `apps/web/src/components/chat/actions_apply.rs:57-73` | ✅ implemented | Both edit paths send `ClientMessage::Edit { doc_id, op, client_id, client_op_id, scope_nonce }`. |
| 16 §Commit Ack | `ServerMessage::Ack` MUST echo `doc_id`, `seq`, `client_op_id` | `apps/web/src/hooks/use_core/effects/message_dispatch_route_protocol.rs:39-48` | ✅ implemented | Ack route destructures `doc_id`, `client_op_id`, plus ignored `..` (including `seq`). |
| 16 §Repo-Scoped Handshake | `ServerMessage::SyncHello` MUST echo `repo_id` | `apps/web/src/hooks/use_core/effects/message_sync.rs:10-31` | ✅ implemented | `handle_sync_hello(peer_id, repo_id, scope_nonce, vector, signals)` requires `repo_id` and matches it against `current_repo_id`. |
| 16 §Repo-Scoped Handshake | `SwitchRepo / SwitchBranch` `switch_nonce` MUST be strictly greater than current `scope_nonce` | `apps/web/src/hooks/use_core/switch_nonce.rs:5-13`, `apps/web/src/hooks/use_core/callbacks_switch_repo.rs:26-37`, `apps/web/src/hooks/use_core/callbacks_switch_branch.rs:33-48` | ✅ implemented | `next_switch_nonce_after()` guarantees monotonic `> current_scope_nonce`. |
| 16 §Repo-Scoped Handshake | Refresh/reconnect/scope restore MUST bind primarily by `repo_id`; `repo_name` only auxiliary | `apps/web/src/hooks/use_core/scope_prefs.rs:69-98` | ✅ implemented | Persistence/restore requires valid UUID `repo_id`; invalid IDs are dropped. |
| 16 §Repo-Scoped Handshake | Remote→Local recovery failure MUST fall back to UUID-first / fail-closed | `apps/web/src/hooks/use_core/scope_prefs.rs:94-97` | ⚠️ partial | Parse is fail-closed on invalid UUID, but no explicit remote-branch-to-local recovery hook was found in `apps/web/src/**`. |
| 16 §Structured Error Contract | WS and HTTP MUST share same error catalog and `code + optional detail` structure | `apps/web/src/i18n/server_error.rs:4-56`, `apps/web/src/hooks/use_core/effects/message_protocol.rs:71-96`, `apps/web/src/components/login/page.rs:118-129` | ⚠️ partial | WS protocol handling is structured (`ServerError { code, detail }`), but HTTP login path also synthesizes raw transport strings, and full shared-catalog proof is outside `apps/web/src/**`. |
| 16 §Structured Error Contract | WS MUST use structured `ProtocolError { error }`, not `Error(String)` | `apps/web/src/hooks/use_core/effects/message_dispatch_route_protocol.rs:22-29`, `apps/web/src/api/socket/socket_types.rs:1-16` | ✅ implemented | Protocol routing matches `ServerMessage::ProtocolError { error, ... }`; no `Error(String)` WS message variant exists in scope. |
| 16 §Structured Error Contract | Source Control user-facing errors MUST use dedicated `SC_*` catalog | `apps/web/src/hooks/use_core/source_control_notice.rs:11-17`, `apps/web/src/hooks/use_core/source_control_notice.rs:31-45`, `apps/web/src/i18n/server_error.rs:22-41` | ✅ implemented | `SourceControlNotice::from_server_error()` only accepts `Sc*` codes; translations map `ScRepoNotSelected`, `ScRemoteBranchReadonly`, etc. |
| 16 §Reconcile Identity | Frontend MUST NOT treat “same content” as the only long-term correctness criterion | `apps/web/src/hooks/use_core/pending.rs:11-23`, `apps/web/src/hooks/use_core/effects/message_dispatch_write.rs:24-39`, `apps/web/src/editor/sync/history.rs:14-19` | ✅ implemented | Pending edits are keyed by `client_id`/`client_op_id`; ack removes by `client_op_id`, not by content string equality. |
