{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-local-backend",
  "description": "Allow the bundled LocalBackend WebView to use host-local backend preference commands.",
  "local": true,
  "windows": ["main"],
  "permissions": ["deve-native-backend-commands:default"]
}
