| Plan §Section | Clause (one line) | Code Location | Status | Evidence |
|---|---|---|---|---|
| `05_network.md §Mobile P2P Strategy` | Foreground Participation: iOS/Android 客户端在前台运行时 **MUST** 作为完整 P2P Peer 参与 Gossip、握手与增量同步。 | — | ❌ missing | `rg -n "iOS|Android|mobile|foreground" apps/cli/src/server` returned no matches relevant to mobile foreground P2P behavior. |
| `05_network.md §Mobile P2P Strategy` | Background Degrade: iOS/Android 进入后台后 **MUST** 立即降级为 **Light Peer**，停止主动 Gossip 轮询与高频广播。 | — | ❌ missing | `rg -n "background|light-peer|Light Peer" apps/cli/src/server` returned no matches. |
| `05_network.md §Mobile P2P Strategy` | Pull-on-Resume: 移动端回到前台时 **MUST** 触发一次 `SyncHello`，并按 Vector Clock 执行缺失补齐。 | — | ❌ missing | `rg -n "resume|foreground|mobile" apps/cli/src/server` returned no matches for mobile resume handling. |
| `05_network.md §Mobile P2P Strategy` | Write Boundary: Light Peer 状态下 **MUST** 禁止发起长时合并与大批量重放，仅保留必要心跳/唤醒能力。 | — | ❌ missing | No `light-peer` mode or mobile write-boundary code appears under `apps/cli/src/server/**`. |
| `05_network.md §Mobile P2P Strategy` | Durability Guarantee: 后台窗口期产生的跨端更新 **MUST** 由 Server（Always-on Relay）托管，移动端前台恢复后再增量拉取。 | — | ❌ missing | No mobile background/foreground durability logic is implemented in `apps/cli/src/server/**`. |
| `05_network.md §Main / Proxy` | 路由契约：浏览器 **MUST** 优先连接当前 origin 下的 `relative /ws`；Proxy 模式必须保持该相对路径可用，而不是要求前端感知后端真实端口。 | [router.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/router.rs#L35), [node_role_http.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/node_role_http.rs#L8) | ✅ implemented | Server exposes `"/ws"` on the main router and also exposes `GET /api/node/role` with `{ role, ws_port, main_port }`. |
| `05_network.md §Main / Proxy` | 端口策略：本地开发环境 **MAY** 暴露显式端口用于诊断；生产环境 **MUST** 通过单一配置端点或反向代理提供稳定入口。 | [node_role_http.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/node_role_http.rs#L8), [mod.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/mod.rs#L175) | ⚠️ partial | Plan: `生产环境 MUST 通过单一配置端点或反向代理提供稳定入口。` Code exposes ports via `Json(serde_json::json!({ "role": r.role, "ws_port": r.ws_port, "main_port": r.main_port, }))` and sets both to the runtime port for main mode. |
| `05_network.md §WebSocket 协议类型` | `SyncHello`, `SyncRequest`, `SyncPush`: P2P 同步协议消息；凡进入同步路径的消息 **MUST** 携带可确定路由的 `repo_id`。 | [ws/route/core.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/route/core.rs#L174), [handlers/sync/hello.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello.rs#L17) | ✅ implemented | `ClientMessage::SyncSnapshotRequest { peer_id, repo_id }`, `ClientMessage::SyncRequest { repo_id, requests }`, `ClientMessage::SyncPush { repo_id, ops }`; `SyncHelloInput` also includes `pub repo_id: RepoId`. |
| `05_network.md §WebLightPeer Handshake` | 后续 `SyncRequest`、`SyncPush`、`SyncSnapshotRequest`、`SyncPushSnapshot` 与实时同步广播均 **MUST** 沿用同一 `repo_id`，否则服务器必须拒绝或断开连接。 | [handlers/sync/hello.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello.rs#L92), [handlers/sync/guard.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/guard.rs#L24), [handlers/sync/transfer.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/transfer.rs#L21), [handlers/sync/snapshot.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/snapshot.rs#L20) | ✅ implemented | `handle_sync_hello` binds `session.bind_repo(repo_id)`; later `require_bound_peer` rejects mismatched repo with `ServerErrorCode::SyncRepoUnbound`. |
| `05_network.md §Deterministic Routing` | `SyncHello` **MUST** 提供 `repo_id` 与当前 vector，确保 Server 能决定是走增量同步还是快照回退。 | [handlers/sync/hello.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello.rs#L17) | ✅ implemented | `SyncHelloInput` requires `remote_vector` and `repo_id`; `handle()` passes both into `engine.handshake(...)`. |
| `05_network.md §Deterministic Routing` | `SyncRequest` **MUST** 至少携带 `{ repo_id, known_vector }`；禁止依赖连接外的隐式默认 repo。 | [ws/route/core.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/route/core.rs#L184), [handlers/sync/transfer.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/transfer.rs#L33) | ⚠️ partial | Plan: ``SyncRequest MUST 至少携带 { repo_id, known_vector }``. Code: `ClientMessage::SyncRequest { repo_id, requests }` and server builds `sync_proto::SyncRequest { peer_id, repo_id, range }`; `known_vector` is not present in server-scope message handling. |
| `05_network.md §Deterministic Routing` | `Snapshot` **MUST** 携带其所属 `repo_id` 与生成时的 server vector；协议示例不得使用空 repo 占位符。 | [handlers/document/open.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/open.rs#L73), [handlers/sync/snapshot.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/snapshot.rs#L48) | ⚠️ partial | Plan: ``Snapshot MUST 携带其所属 repo_id 与生成时的 server vector``. Code sends `ServerMessage::Snapshot { repo_id: scope.repo_id, ... base_seq, version, delta_ops }` and `ServerMessage::SyncPushSnapshot { repo_id: response.repo_id, ... ops }`; `repo_id` is present but no explicit `server vector` is emitted here. |
| `05_network.md §Sync Process / Security` | Encryption: 所有 Repo 数据在传输前 **MUST** 使用 `RepoKey` 加密 (AES-256-GCM)。 | [handlers/key_exchange.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/key_exchange.rs#L4), [handlers/sync/transfer.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/transfer.rs#L73) | ⚠️ partial | Server hands out `RepoKey` over authenticated WSS and sync transfer uses `Vec<EncryptedOp>`, but no AES-256-GCM enforcement appears in `apps/cli/src/server/**`. |
| `05_network.md §Sync Process / Logic` | **Update VC**: B 成功写入后，**MUST** 更新本地记录的 $VC_{PeerA}$ 至最新 Seq。 | — | ❌ missing | No vector-clock update logic is implemented in `apps/cli/src/server/**`; `handle_push` only logs `Applied {} ops for repo {}` after `engine.apply_remote_ops(...)`. |
| `05_network.md §Sync Process / Logic` | **Direct Write**: B 作为镜像端，**MUST** 直接接受来自 A 的已校验数据（无需本地冲突消解，因为 B 是只读的）。 | [handlers/sync/transfer.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/transfer.rs#L86), [handlers/sync/snapshot.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/snapshot.rs#L87) | ⚠️ partial | Server forwards verified sync payloads into `engine.apply_remote_ops(...)` / `engine.apply_remote_snapshot(...)`, but the no-conflict direct-write rule itself is implemented outside `apps/cli/src/server/**`. |
| `05_network.md §WebSocket Reconnection` | State Recovery: 重连成功后 MUST 重新发送当前 repo 的 `SyncHello` 获取离线期间的变更；若用户已切换 repo，则必须以新 `repo_id` 重建连接上下文。 | — | ❌ missing | This is client reconnection behavior; no reconnect initiator exists under `apps/cli/src/server/**`. |
| `05_network.md §WebSocket Reconnection` | Auth Boundary: 若服务端明确返回 `401/403` 或 `AUTH_*` 结构化错误，客户端 **MUST** 停止普通重连循环并转入 `Unauthorized` 处理；见 `09_auth.md`。 | [auth/middleware.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/auth/middleware.rs#L56), [ws/mod.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/mod.rs#L45) | ⚠️ partial | HTTP middleware returns structured `AuthErrorResponse::new(code)` with `401`; WS handshake returns plain text `(StatusCode::UNAUTHORIZED, "Unauthorized")`, not `AUTH_*` JSON. |
| `05_network.md §Indirect Sync & Trust Boundary` | B **MUST Ignore** A's offer (Strict Filtering). | — | ❌ missing | No gossip-offer filtering code for indirect relay trust is present under `apps/cli/src/server/**`. |
| `05_network.md §Indirect Sync & Trust Boundary` | C **MUST NOT** 传输 A 的数据给 B (Payload Blocking). | — | ❌ missing | No relay payload-blocking logic for stranger peers is implemented in `apps/cli/src/server/**`. |
| `05_network.md §Indirect Sync & Trust Boundary` | B **MUST** 将其写入 `ledger/remotes/peer_a/`，绝不可写入 `peer_c`。 | — | ❌ missing | No source-signature-based remote storage attribution logic is visible under `apps/cli/src/server/**`. |
| `05_network.md §本章相关配置` | Mobile Override: 移动端（iOS/Android）后台状态下，`SYNC_MODE` 设置 **MUST** 被强制覆盖为 `light-peer` 模式。 | — | ❌ missing | `apps/cli/src/server/mod.rs` starts `RepoScopedSyncEngine::new(..., SyncMode::Auto)` with no mobile/background override. |
| `09_auth.md §Auth-Specific Invariants` | `session expired / token missing` **MUST** 进入 `Unauthorized`，而不是继续显示普通断网重连。 | [auth/middleware.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/auth/middleware.rs#L56), [ws/mod.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/mod.rs#L45) | ⚠️ partial | Server returns unauthorized on missing/invalid token, but only HTTP is structured: `unauthorized(AuthErrorCode::TokenMissing)` / `TokenExpired`; WS handshake returns plain `"Unauthorized"`. |
| `09_auth.md §Unauthorized vs Disconnected` | **Unauthorized**：JWT 缺失、过期、撤销或服务端明确拒绝当前会话。客户端 **MUST** 立即退出写态，跳转登录页或显示认证失效界面。 | — | ❌ missing | This client state-machine behavior is not implemented in `apps/cli/src/server/**`. |
| `09_auth.md §Unauthorized vs Disconnected` | **Rule**：`401/403` 与 `AUTH_*` 错误码 **MUST NOT** 被重新包装成普通断网状态。 | [handlers/source_control/errors.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/source_control/errors.rs#L32), [ws/mod.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/mod.rs#L45) | ⚠️ partial | HTTP keeps auth-specific status mapping (`AuthTokenExpired | AuthTokenMissing => StatusCode::UNAUTHORIZED`), but WS handshake still emits plain `"Unauthorized"` rather than a structured auth error object. |
| `09_auth.md §Access Control` | 浏览器 WebLightPeer **MUST NOT** 写入这些文件，而是使用 `WebCrypto + IndexedDB`。 | [handlers/key_exchange.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/key_exchange.rs#L4) | ⚠️ partial | Server-side evidence only shows `RepoKey` delivery over authenticated WSS and says `RepoKey 仅在内存中存在于客户端`; no WebCrypto/IndexedDB enforcement exists in this scope. |
| `09_auth.md §Security Policies / CORS` | 生产环境 (Production): Origin 限制为用户配置的域名白名单，**MUST NOT** 使用 `allow_origin(Any)`。 | [setup.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/setup.rs#L22) | ✅ implemented | CORS uses `allow_origin(AllowOrigin::list(origins))`; `*` is filtered out at parse time. |
| `09_auth.md §Security Policies / CORS` | 开发环境 (Development): **MAY** 放宽为 `http://localhost:{port}` 和 `http://127.0.0.1:{port}`，但 **MUST** 在日志中显著标记 `⚠ CORS: Dev-Mode (Relaxed)`。 | [setup.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/setup.rs#L17) | ⚠️ partial | Plan: ``MUST 在日志中显著标记 `⚠ CORS: Dev-Mode (Relaxed)` ``. Code logs `WARNING: CORS development allow list active`; it does not emit the planned verbatim message or synthesize localhost defaults. |
| `09_auth.md §Security Policies` | Security Headers: 所有 HTTP 响应 **MUST** 包含 `X-Content-Type-Options`, `X-Frame-Options`, `Content-Security-Policy`。 | [auth/headers.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/auth/headers.rs#L14), [router.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/router.rs#L94) | ✅ implemented | `security_headers` inserts all three headers and `router.rs` layers it over the app. |
| `09_auth.md §Security Policies` | Key File Permissions: `identity.key` 文件权限 **MUST** 设为 `0600` (Owner-only)。 | [security.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/security.rs#L21) | ❌ missing | `load_or_generate_identity_key()` delegates to core and logs the file path; no `set_permissions`, `PermissionsExt`, or `0600` handling exists under `apps/cli/src/server/**`. |
| `09_auth.md §Security Policies` | Audit Log: 登录成功/失败事件 **MUST** 记录到结构化日志 (Tracing)，包含 IP、User-Agent、Timestamp。 | [auth/handlers/login.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/auth/handlers/login.rs#L18), [auth/handlers/session.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/auth/handlers/session.rs#L56) | ⚠️ partial | Code logs `user` and `ip` on success/failure, but there is no `User-Agent` extraction in login/session handlers. |
| `09_auth.md §本章相关配置` | `AUTH_SECRET`: JWT 签名密钥 (MUST >= 32 字节)。生产环境必须显式设置。 | [router.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/router.rs#L102) | ⚠️ partial | `router::load_auth_config()` exits on `AuthConfig::from_env()` error, but the `>= 32` check is delegated outside `apps/cli/src/server/**`. |
| `12_commands.md §Backend 命令可见性` | 条件不满足时，系统 **MUST** 保持 `native` 后端，并对用户给出明确说明，而不是静默尝试拉起 CLI。 | [agent_bridge.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/agent_bridge.rs#L10) | ⚠️ partial | Plan: `系统 MUST 保持 native 后端，并对用户给出明确说明，而不是静默尝试拉起 CLI。` Code: `const DEFAULT_CLI: &str = "opencode";` and `std::env::var("AGENT_CLI_PATH").unwrap_or_else(|_| DEFAULT_CLI.to_string())`, so server silently falls back to an external CLI path. |
| `13_settings.md §Environment Variables` | `AUTH_SECRET` ... **生产环境 MUST 设置**。 | [router.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/router.rs#L102) | ⚠️ partial | Server exits if `AuthConfig::from_env()` fails, but explicit production enforcement lives behind delegated config loading outside this scope. |
| `13_settings.md §Environment Variables` | `AUTH_PASS` ... 生产环境 MUST 设置。 | [router.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/router.rs#L102) | ⚠️ partial | Same server-scope evidence as `AUTH_SECRET`: failure exits, but direct production-password validation is delegated outside `apps/cli/src/server/**`. |
| `13_settings.md §Environment Variables` | `ALLOWED_ORIGINS` ... 生产环境 MUST 显式设置。 | [setup.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/setup.rs#L35) | ⚠️ partial | Plan: `生产环境 MUST 显式设置。` Code: `let Ok(origins) = std::env::var("ALLOWED_ORIGINS") else { return Ok(Vec::new()); };` so empty allow-list is accepted without production-only enforcement here. |
| `13_settings.md §AI` | `ai.agent_bridge.trusted` ... 未确认时 MUST NOT 起 CLI。 | [agent_bridge.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/agent_bridge.rs#L24) | ⚠️ partial | Plan: `未确认时 MUST NOT 起 CLI。` Code launches CLI from `AGENT_CLI_PATH` or default `opencode` without checking any `trusted` flag. |
| `13_settings.md §AI` | 任一条件不满足时，系统 **MUST** 自动退回 `ai.mode = native`，并向用户显示明确原因。 | [agent_bridge.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/agent_bridge.rs#L24) | ❌ missing | No `ai.mode` fallback logic exists under `apps/cli/src/server/**`; `handle_agent_chat()` always attempts CLI spawn. |
| `16_web_thin_client_ledger.md §Non-Negotiable Invariants` | Web MUST NOT 持有“已确认业务状态”的私有副本。 | — | ❌ missing | This is a frontend storage rule; no browser-state enforcement code exists in `apps/cli/src/server/**`. |
| `16_web_thin_client_ledger.md §Non-Negotiable Invariants` | `Auth for Write` 与 `Handshake for Sync` MUST 分离建模。 | [ws/mod.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/mod.rs#L25), [handlers/sync/hello.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello.rs#L26), [session_scope.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session_scope.rs#L21) | ✅ implemented | WS handshake validates JWT/Cookie first; later sync handshake binds `authenticated_peer_id`, `bound_repo_id`, and separate `writer_identity`. |
| `16_web_thin_client_ledger.md §Non-Negotiable Invariants` | 写入就绪状态 MUST 是 repo-scoped，而不是 connection-scoped 猜测值。 | [session.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session.rs#L46), [session_scope.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session_scope.rs#L21), [handlers/sync/hello_scope.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello_scope.rs#L111) | ✅ implemented | Session tracks `bound_repo_id`, `writer_identity.repo_id`, `active_repo_id`, and browser `SyncHello` rejects mismatched `requested_repo_id`. |
| `16_web_thin_client_ledger.md §Non-Negotiable Invariants` | 文件切换、重连、快照刷新都 MUST 从 `confirmed + pending overlay` 重建。 | [handlers/document/open.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/open.rs#L41), [handlers/document/confirmed.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/confirmed.rs#L18), [handlers/document/history.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/history.rs#L31) | ⚠️ partial | Server supplies confirmed `Snapshot`, `History`, and origin-tagged `ConfirmedOp`; pending overlay reconstruction itself is not implemented in server scope. |
| `16_web_thin_client_ledger.md §Non-Negotiable Invariants` | Commit、Delete、Merge 的最终成立条件 MUST 是 ledger append / ledger anchor，而不是 metadata 或 snapshot 副作用。 | [handlers/docs/delete.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/docs/delete.rs#L50), [handlers/merge/peer.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/merge/peer.rs#L81), [handlers/source_control/http_mutations_commit.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/source_control/http_mutations_commit.rs#L15) | ⚠️ partial | Delete and merge append structure/content facts before projection refresh; commit is delegated to `repo.commit_staged_in_repo(...)` outside this scope, so the full invariant is not fully evidenced here. |
| `16_web_thin_client_ledger.md §Edit Intent` | `ClientMessage::Edit` MUST 携带 `doc_id`, `op`, `client_id`, `client_op_id`。 | [ws/route/core.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/route/core.rs#L48) | ✅ implemented | Match arm destructures `ClientMessage::Edit { doc_id, op, client_id, client_op_id, scope_nonce }`. |
| `16_web_thin_client_ledger.md §Commit Acknowledgement` | `ServerMessage::Ack` MUST 回显 `doc_id`, `seq`, `client_op_id`。 | [handlers/document/edit.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/edit.rs#L75) | ✅ implemented | Both ack paths emit `ServerMessage::Ack { ... doc_id, seq, client_op_id }`. |
| `16_web_thin_client_ledger.md §Repo-Scoped Handshake` | `ServerMessage::SyncHello` MUST 回显 `repo_id`。 | [handlers/sync/hello.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/sync/hello.rs#L111) | ✅ implemented | Server emits `ServerMessage::SyncHello { peer_id: local_peer_id, repo_id, scope_nonce, ... }`. |
| `16_web_thin_client_ledger.md §Repo-Scoped Handshake` | `SwitchRepo / SwitchBranch` 的 `switch_nonce` MUST 严格大于当前 `scope_nonce`。 | [handlers/switcher_guard.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/switcher_guard.rs#L22) | ✅ implemented | `if switch_nonce > session.scope_nonce() { return true; }` else emits stale-scope `ProtocolError`. |
| `16_web_thin_client_ledger.md §Repo-Scoped Handshake` | 浏览器刷新、重连与 scope 恢复 MUST 以 `repo_id` 作为主绑定键，`repo_name` 仅可作辅助提示或兼容恢复。 | [session_scope.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session_scope.rs#L110), [session_test.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session_test.rs#L43), [repo_scope_selector.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/repo_scope_selector.rs#L53) | ✅ implemented | `active_db_matches_scope()` prefers `repo_id`; tests assert `active_db_prefers_repo_id_over_name()`. |
| `16_web_thin_client_ledger.md §Repo-Scoped Handshake` | 当用户从 `Remote Branch` 切回 `Local` 时 ... 解析失败时 MUST 回退到 UUID-First / Fail-Closed 规则。 | [session_repo.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/session_repo.rs#L5), [handlers/switcher_branch_hint.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/switcher_branch_hint.rs#L22), [repo_scope_lookup.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/repo_scope_lookup.rs#L33), [repo_scope_remote.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/repo_scope_remote.rs#L39), [switcher_last_local_repo_test.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/switcher_last_local_repo_test.rs#L20), [switcher_exact_selector_fail_closed_test.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/switcher_exact_selector_fail_closed_test.rs#L21) | ✅ implemented | Session preserves `last_local_repo(_id)`, branch switch restores it, and stale UUID/selector paths fail closed with `ScRepoContextInvalid`. |
| `16_web_thin_client_ledger.md §Structured Error Contract` | `WebSocket` 与 `HTTP` MUST 共享同一错误码目录与同一 `code + optional detail` 结构。 | [channel.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/channel.rs#L65), [handlers/source_control/errors.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/source_control/errors.rs#L24), [source_control_proxy_http.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/source_control_proxy_http.rs#L67) | ✅ implemented | WS `ProtocolError` wraps `ServerError`; HTTP returns `Json(error)` where `error` is the same `ServerError { code, detail }` structure. |
| `16_web_thin_client_ledger.md §Structured Error Contract` | `WebSocket` MUST 使用结构化 `ProtocolError { error }`；不得新增 `Error(String)` 作为实现目标。 | [channel.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/channel.rs#L65), [ws/receive.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/ws/receive.rs#L65) | ✅ implemented | WS error sends are centralized to `ServerMessage::ProtocolError { error, switch_nonce, scope_nonce }`; no server-scope `Error(String)` transport was found. |
| `16_web_thin_client_ledger.md §Structured Error Contract` | `Source Control` 用户态错误 MUST 使用独立 `SC_*` 目录。 | [handlers/source_control/errors/map.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/source_control/errors/map.rs#L45), [handlers/source_control/errors.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/source_control/errors.rs#L32), [source_control_proxy_http_target.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/source_control_proxy_http_target.rs#L21) | ✅ implemented | Mapping emits `ScPendingNotFound`, `ScStagedNotFound`, `ScDocNotFound`, `ScCommitNotFound`, `ScNothingToCommit`, `ScRepoContextInvalid`, etc. |
| `16_web_thin_client_ledger.md §Reconcile Identity` | 前端 MAY 基于内容做临时比对，但 MUST NOT 将“内容相同”当成长期正确的唯一判定方式。 | [handlers/document/edit.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/edit.rs#L60), [handlers/document/confirmed.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/confirmed.rs#L29), [handlers/document/history.rs](/home/develata/gitclone/Deve-Notebook/apps/cli/src/server/handlers/document/history.rs#L31) | ✅ implemented | Server persists and returns `client_id/client_op_id` via `LedgerEntry`, `ConfirmedOp::new(..., Some(ClientOrigin { ... }))`, `History`, `NewOp`, and `Ack`, so reconcile is not content-only. |
